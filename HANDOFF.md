# Handoff

> Generated by `ln-handoff` at 2026-07-08 17:57 (UTC+2). Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Land the last FE-1169 card (E, theme demo), run the one outer manual beat session, then tie off the frontier (restack → PR #305 description → submit). The frontier went from freshly-scoped to one-card-remaining in a single day via parallel builders coordinated through this thread.

## Session State

- **Session shape**: this was a coordination thread — grill → plan → scope (5 files, merged against two parallel second-look scopings) → parallel builder dispatch/reintegration (11 builder commits merged from cursor/codex worktrees) → `ln-judo-review` (2 fix cards, both consumed) → `ln-sync` → this handoff.
- **Last completed action**: D3 merge (`8b343ca9`) + capture-probe verification of its synthetic ask pair.
- **Current skill**: `ln-handoff`.

## In-flight work

### FE-1169 remaining sequence (canonical order in PLAN pointer; extra context here)

1. **E — `memory/cards/main-editor-chrome--theme-demo-expansion.md`** (last card, light/earned). Builder notes that live only in chat:
   - The consult menu's surface-identity channel reuses the existing `borderAccent` theme role rather than a named role (`consult-menu.ts:32`, `SURFACE_IDENTITY_BORDER_ROLE = 'borderAccent'`). E's gallery walk should render it under the surface-identity heading and decide: keep `borderAccent` or promote a named role. If promoted, that's a small SPEC/lexicon touch (border-semantics vocabulary).
   - Mode roles are `modeSpecifyBorder`/`modeExecuteBorder` (`mode-border-theme.ts`, `satisfies Record<OperationalModeId, …>` — a third mode fails compilation without a role). E must enumerate roles from theme data, not hardcode.
2. **Outer manual beat session** (single session covers all; workbench `.fixtures/workbenches/workspace-alpha-grounding`, reseed `npm run seed -- --seed workspace-alpha-grounding/base --reset`):
   - physical-terminal wheel smoke (iTerm2/Kitty/Ghostty) — FE-1115 residual, absorbed obligation
   - live mode-switch beat: shift+tab cycles, alt+m opens picker, footer/editor labels update; verify plain `pi` in another terminal still has shift+tab thinking-cycle (in-process scoping proof)
   - gallery walk `npm run dev:components` in both themes (incl. candidates preview — B's builder skipped the interactive check)
   - D3 beat: root-esc an ask → status hint names `/brunch:continue` → run it → answer → one durable synthetic pair
   - render-height/focus check on the persistent editor (thread-1 tracer's outer leg)
3. **Tie-off**: `gt restack` (parents merged to `next`), PR #305 (title `FE-1169: Main editor chrome and TUI component UX & rendering`), description written at tie-off per convention; update Linear FE-1169 description if the six-thread framing needs post-build corrections.

### B2 — review-set details renderer (named fast-follow, currently scoped nowhere)

The deleted `--details-driven-rendering.md` card carried this; its guidance now lives only here:

- Scope after/with E or post-tie-off as its own slice: `present_review_set` (and assess `present_digest`) adopt details-backed rendering following B's pattern.
- Reuse `src/.pi/extensions/exchanges/shared/details-rendering.ts` (parse-or-fallback helper built for exactly this).
- Carry the **proposal-vocabulary obligation**: review-set rendering must not visually imply graph commitment; acceptance/commit language stays on the terminal ask path.
- Per the reactivated SPEC verification note: each details-backed adopter brings its renderResult golden family (per-component render tests, width/theme matrix) — see SPEC §Verification Design, exchange-presentation oracle compound, item 1.
- D104-L row already names the fence: digest/review-set/ask stay pass-through "until separately scoped".

### Review debt

All `ln-judo-review` findings (2026-07-08, over A1/A2/C1/D1) are **addressed**: findings 1–2 via `--keybinding-scope-fix.md` (consumed, `8eeaab02`), findings 3–5 via `--ask-step-refactor.md` (consumed, `b13e60f9`). One standing watch, not debt: **finding 6 — `ask.ts` is now at 810 lines** (759 at review, +D3's re-present entry). The named trigger: approaching ~1k → fractal split (`ask.ts` public root, collectors into `ask/`). B2 does not touch ask.ts; the next ask-surface slice should check this first.

### Flake watch

`roving-suite-flake` closed by same-day diagnosis (PLAN §Retired has the record): `npm test` capped at `--maxWorkers=4`, promotion fixture de-churned. Evidence since: two clean full-suite `verify` runs (B and D3 builders) + one clean `npm test` here. Re-open condition: any new `git-host-promotion-port` timeout under the cap.

## Decisions and assumptions

| Item | Status |
| --- | --- |
| D104-L revision (two render sources, render-honesty retained, fallback rule) | persisted (SPEC row + topology homes, B's commit) |
| renderResult golden family partially reactivated for details-backed adopters | persisted (SPEC §Verification Design, `42fb5a30`) |
| Hierarchical esc semantics (root cancel / nested back) + `/brunch:continue` hint loop | persisted (code + STRUCTURED_EXCHANGE_ANSWERING_PATHS.md) |
| `borderAccent` as surface-identity role | **volatile** — implicit in code; E decides keep-or-promote |
| `develop-mode` split to Horizon (D98-L/req-26/D40-L revision path at pickup) | persisted (PLAN Horizon row) |

## Diagnostic evidence

- **Flake diagnosis**: full-suite-only timeout; phase timestamps showed cumulative git-spawn slowdown under unbounded vitest workers, no `git apply` hang; isolation always green. Fix = worker cap + fixture de-churn; witness preserved (port applies literal patch via stdin — `git-host-promotion-port.ts:48-56` — so the fixture's shared object db is irrelevant).
- **A3 golden trailing-newline failure** was pre-existing in the builder worktree (failed there too), not a merge artifact; fixed by regeneration (`202fcd6a`), `sem` confirmed no semantic change.
- **False-dirty scare** during B's merge: `git status` showed SPEC modified but hashes matched index — stale stat cache, cleared by `git update-index --refresh`. Not real dirt.
- **D3 synthetic pair**: capture probes + family-completeness ran green here post-merge (the builder's own list didn't include them).

## Repo state

- **Branch**: `ln/fe-1169-editor-chrome` at `8b343ca9`, clean tree, PR #305 open. Parents (`ln/fe-1164-ask-terminal` → … → `next`) all merged; `gt restack` owed before submit.
- **Interleaved user commit**: `81109c60 "initial spec additions re sweep oracling"` — user-authored FE-1163 oracle prep on this branch; state/intent not discussed in this thread. Do not touch; ask the user when FE-1163 scoping starts.
- **Merged-and-integrated builder branches** (all content on this branch; candidates for deletion with user approval): local `cursor/{5230f3fc, ba783af8, c72fce92, 26102108, a46a65b4, 9dcf1d15, 49b30ec2, d51bc7c2, b821e7bf}`; codex worktrees `~/.codex/worktrees/{dc43,0959}/brunch` (prunable, sandbox-blocked for reads); cursor worktrees under `~/.cursor/worktrees/brunch-next/`.
- **Tests**: last full `verify` green (D3 builder); focused suites green here after every merge.
- **FE-1159**: merged; npm publish still human-gated (mechanics in PLAN definition).

## Artifact status

| Artifact | State |
| --- | --- |
| memory/SPEC.md | current (D104-L revised, golden-family note, user's sweep-oracling additions) |
| memory/PLAN.md | current (synced 17:25 + pointer updated through D3) |
| memory/cards/ | 4 files: `main-editor-chrome--theme-demo-expansion.md` (live, next build); `tool-schema-convergence--ledger.md` (FE-1163, next frontier); `walkthrough-batch-2--seed-variants.md` + `executor-run-integrity--plan-projection.md` (FE-1167 inputs) |
| HANDOFF.md | this file |

## Next steps

1. Build E: `ln-build memory/cards/main-editor-chrome--theme-demo-expansion.md` (carry the borderAccent decision note above into the builder prompt).
2. Run the outer manual beat session (checklist above); record findings in `TESTING_FINDINGS.md`.
3. Tie off FE-1169: `gt restack` → PR #305 description → submit (read `docs/praxis/graphite-workflow.md` first).
4. Scope B2 (review-set renderer) — before or after tie-off, user's call.
5. Then per PLAN: FE-1163 build (off `next`; user's `81109c60` oracle prep pending), FE-1167 after FE-1169 lands, FE-1159 publish when npm auth is available.

## Resume prompt

Paste into a new session:

> Read `HANDOFF.md` in the workspace root. FE-1169 (`ln/fe-1169-editor-chrome`, PR #305) is one card from code-complete.
> Next action: build `memory/cards/main-editor-chrome--theme-demo-expansion.md` (note the borderAccent surface-identity decision in the handoff's In-flight section), then the outer manual beat session, then tie-off. Ask the user which to start with if they're present; default to building the card.
