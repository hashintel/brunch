# Handoff

> Generated by `ln-handoff` at 2026-07-07 14:58 (UTC+2). Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.
> Supersedes the 2026-07-06 handoff (LN/KA seam reconciliation) — the KA stack has since merged to `next`; only its unresolved decision items are carried forward below.

## Goal

Close out the `alpha-release-readiness` frontier (FE-1159): the inner loop is fully built and committed; what remains is outer-loop evidence (manual walkthrough, A38-L conduct/latency), the roving test-flake diagnosis, and the actual alpha publish.

## Session State

- **Last completed skill**: `ln-build` acceptance — the no-auth-gate slice was verified against its card and committed (`d86b58b6`). All five FE-1159 threads are built.
- **Current skill**: `ln-handoff`.
- **Flow position**: consult → spike → grill → spec → plan → scope → build ✓ → (outer walkthrough → review/tie-off pending).
- **This session's full arc (2026-07-07)**: ln-consult → ln-spike (packaging/auth evidence) → ln-grill (auth design settled) → ln-spec (D113-L/D114-L/D115-L, req 29, A38-L, I59-L) → ln-plan (FE-1159 admitted; issue created; branch renamed via `gt rename`) → ln-scope (4 scope files total) → parallel builder threads built all of it → acceptance reviews → 5 commits.

## In-flight work

### FE-1159 remaining (not build work — evidence + sequencing)

1. **Outer walkthrough — DONE 2026-07-07 (this session)**: full evidence recorded in the FE-1159 PLAN entry (§Outer walkthrough evidence). No-auth boot, `brunch login` (OpenRouter key + Anthropic key), fresh boots on both allowlist entries, junctures firing, live elicitation + graph writes all verified. **The walkthrough found and fixed a release-blocking defect**: `read_graph`'s params schema carried a top-level `oneOf` that Anthropic-family backends reject with a 400 on *every* provider turn — invisible to the faux-provider suite. Fixed + regression oracle added, committed as `FE-1159: Drop read_graph top-level oneOf…`.
2. **A38-L validation — initial evidence recorded (PLAN)**: kick ≈11s (OpenRouter) / ≈14.5s (Anthropic direct), question turn ≈13s, `thinking: low`, conduct clean. Revision candidates `openai/gpt-5.5`, `openai/gpt-5.4-mini` are now recorded durably in the PLAN entry's execution pointer.
3. **Publish sequencing drift — fixed**: the stale "stack must merge first" note in PLAN (entry + dependencies block) was rewritten this session. Publish = merge fe-1159 → `next`, then `npm publish` (tag `alpha`, `prepack` runs the full build; `npm run check:release-pack` is the pre-publish smoke).

### Roving full-suite test flake (diagnosis pending — route to `ln-diagnose`)

Evidence from this session:

- Builder 2 hit a 15s per-test timeout on `src/app/__tests__/git-host-promotion-port.test.ts` ("applies a real promoted git patch…") during full-suite `npm run verify` and skipped it. **Skip rejected and reverted at acceptance** — the test passes in isolation (~600ms, ran twice) and in full suite.
- My own first full-suite run failed a *different* test (`src/probes/__tests__/structured-exchange-ordering-proof.test.ts`), which passed in isolation and in the second full run (1501 passed).
- Conclusion: intermittent timing flake under full-suite parallel load that lands on different heavy tests per run; no test is currently skipped; no defect in either test. Heads-up owed to KA (his host-promotion oracle was the victim this time).

### Acceptance-review residue (small, non-blocking)

- **Release smoke scope boundary**: `check:release-pack` boots `--mode print` from an empty foreign cwd, which does not exercise the better-sqlite3 native binding (no workspace DB opened). A blocked/broken native build could slip past. Extend the smoke to a DB-touching path when one is cheap.
- **Registry-wrapper ceiling** is now marked in code (`src/app/model-policy.ts`, `ceiling:` comment): the wrapper must forward any new mutating pi registry method or upgrades shadow-write onto the wrapper.
- **Login exit-report registry**: fixed during acceptance from `ModelRegistry.create()` (reads ambient models.json) to `inMemory` so the report resolves exactly like session boot (D39-L). Committed in `7fe5961e`.

### Carried forward from the 2026-07-06 handoff (KA items, still undecided)

The KA stack is merged, but three decision items from the prior handoff remain unresolved in PLAN:

1. FE-1107 close-or-narrow (residue: `executor.md` still teaches the `orchestrator_stub` boundary).
2. KA's `memory/cards/executor-*` disposition — `memory/cards/` still holds `executor-run-integrity--plan-projection.md` and `orchestrator-tool-port--executor-surface-reconciliation.md` (his artifacts; confirm-then-GC, do not delete unilaterally).
3. FE-1134/FE-1137 outer walkthrough evidence — could now share a walkthrough branch/session with FE-1159's outer loop (item 1 above).

## Decisions and assumptions

| Item | Type | Status | Source |
| ---- | ---- | ------ | ------ |
| D113-L (pinned `{provider, model, thinking}` allowlist via Brunch-owned ModelRegistry), D114-L (auth rides Pi's auth.json; `brunch login`; XDG home deferred), D115-L (no-auth upstream gate), req 29, A38-L, I59-L (covered) | decisions/invariants | persisted (SPEC) | this session |
| Allowlist entries: sonnet-4-6 direct + via OpenRouter, `thinking: low` | decision | persisted (`src/app/model-policy.ts`) | user, this session |
| gpt-5.5 / gpt-5.4-mini as post-A38-L revision candidates | note | **volatile (this file only)** | user, this session |
| Roving-flake diagnosis needed; no skips in place | finding | **volatile (this file only)** | acceptance review |
| PLAN "stack must merge first" sequencing note is stale | drift | **volatile (this file only)** | user, session end |

## Repo state

- **Branch**: `ln/fe-1159-alpha-release`, stacked **directly on `next`** (KA stack merged). `ln/fe-1115-tui-refinements-1` is stacked on top of this branch and under active development (TUI theme/preview work, commits minutes old) — coordinate before rebasing/amending fe-1159.
- **FE-1159 commits (top down)**: `d86b58b6` no-auth gate · `7fe5961e` brunch login · `0f7d1d2a` release-pack smoke · `eb6468fb` model allowlist · `e51c264c` packaging fixes. Below them the branch carries four pre-rename FE-1107/executor-reconciliation commits (`ced3f1a2`, `3aa79337`, `deed3f62`, `0a03e7d9`) from its prior life as `ln/fe-xxx-executor-wiring`.
- **SHAs churn under gt restacks** — find commits by the `FE-1159:` message prefix, not by SHA.
- **Worktree**: clean. **Not pushed / no PR yet.**
- **Tests**: full suite green (1501 passed, 2 intentionally-skipped pre-existing) as of `d86b58b6`; subject to the roving flake above.
- **memory/cards/**: only KA's two executor cards + `walkthrough-batch-2--seed-variants.md` remain; all four FE-1159 scope files were consumed and deleted.

## Artifact status

| Artifact | Exists | Current vs conversation |
| --- | --- | --- |
| memory/SPEC.md | yes | current (D113–D115, req 29, A38-L, I59-L covered) |
| memory/PLAN.md | yes | current except the stale "stack must merge first" sequencing note on FE-1159 |
| memory/cards/ | 3 files | none are FE-1159's; two are KA's (confirm-then-GC), one is FE-1124's active card |
| HANDOFF.md | this file | supersedes 2026-07-06 handoff |

## Next steps

1. ~~Run the FE-1159 outer walkthrough~~ — done 2026-07-07; evidence in PLAN. (FE-1134/FE-1137 walkthrough evidence was *not* covered by this session's run — it exercised the auth/model seam, not their beats.)
2. **PR tie-off for `ln/fe-1159-alpha-release`** (`gt submit`; PR title `FE-1159: Alpha release readiness: packaging, model allowlist, auth onboarding`) — description written at tie-off per repo convention.
3. **`ln-plan`/`ln-sync` touch-up**: ~~stale publish-sequencing note~~ (fixed); decide FE-1107 disposition + KA card GC (needs KA).
4. Optional: `ln-diagnose` on the roving full-suite flake.
5. When ready to publish: merge to `next`, `npm run check:release-pack`, then `npm publish` (publishConfig tag `alpha`).

## Retirement rule

Delete or overwrite this file once the outer walkthrough evidence is recorded in PLAN and the FE-1159 PR is submitted (steps 1–3 absorbed).

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root. FE-1159 (alpha-release-readiness) is fully built and committed on `ln/fe-1159-alpha-release` (stacked on `next`); the remaining work is outer-loop evidence and tie-off.
> Start with the outer walkthrough (Next steps 1) or the PR tie-off (step 2), whichever the user prefers. Note the stale publish-sequencing line in PLAN and the volatile items in §Decisions (gpt-5.5/5.4-mini candidates, roving flake) — reconcile them into PLAN/SPEC when touched.
