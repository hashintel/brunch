# Handoff

> Generated by `ln-handoff` at 2026-07-06 18:05 (UTC+2). Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Reconcile the post-rebase seam between LN's 7-branch stack (`ln/fe-1124-…` → `ln/fe-1152-refinements`) and KA's executor lane already on `next` (FE-1089/1109/1111/1112/1118/1125), then present a demo/overview to KA of everything the stack lands.

## Session State

- **Last completed skill**: `ln-sync` — registered KA's executor lane in SPEC (D111-L, D112-L, I58-L; `ExecutionPorts` lexicon row), repaired the decision-ID collision in his code/topology (D101-L/D102-L/I56-L → new IDs), re-baselined `orchestrator-tool-port` (FE-1107) in PLAN, retired the stale FE-1109 "pending ports" blockquote. Also produced `docs/DEMO_STACK_OVERVIEW_2026-07-06.md`. All committed in `066e06c3`.
- **Current skill**: `ln-handoff`
- **Flow position**: `… → sync ✓ → (demo session with KA) → plan` — an `ln-plan` pass is deliberately **deferred** until after the KA conversation (see In-flight work).
- **Handoff trigger**: user-requested at session end.

## In-flight work

> These artifacts exist only in the prior conversation, not on disk (the demo doc carries some of them; the rest is here).

### Seam map (engine/cockpit framing)

- **KA's territory (landed on `next` before `fe-1124`)**: the entire execute-mode run lifecycle as pure core in `src/executor/` — plan check/outline → `plan.yaml` → run metadata → worktree → populate/source-copy → reports → slice execute → sealed-worker agent runner → test result → complete → Petri export → run-local promotion → host-promotion preflight/apply — plus the `drive()` run driver (FE-1125), injected ports in `src/app/*-port.ts`, and Pi tool adapters `execute-orchestrate` / `execute-promotion-prepare` / `execute-host-promotion`.
- **LN's territory (the stack, ~169 files)**: deterministic orientation (FE-1134 dialog/junctures; FE-1137 concentric authority + CODE-mode J5 menu + readiness conduct), closed capture-ingest arc (FE-1135, FE-1136 `present_digest`), closed exchange-presentation arc (FE-1138 bordered answering chrome), FE-1152 refinements (themes, konsistent, boot-kick ordering, orientation-append hardening).
- **Load-bearing shared files (16 overlap; 4 sharpest)**: `src/agents/runtime/executor/active-tools.ts` (his `execute_*` grants now live inside the FE-1137 concentric contract `executor ⊇ elicitor`), `src/agents/prompts/executor.md` + `compose-prompt.ts`, `src/app/pi-extensions.ts` (his ports + LN's `sessionOrientation` wiring meet in one factory), `src/.pi/extensions/__tests__/registry.test.ts` + the authority-matrix test family.
- **Demo arc** (five beats, in the demo doc): cold open → J1 orientation dialog → SPEC-mode elicitation with new bordered chrome → digest flow (paste → digest → request-changes → regenerate → approve with graph receipt) → `/switch` to CODE (in-flight abort + J5 readiness menu, thin vs rich seed) → hand controls to KA at `execute_plan_check` / his orchestrate tooling.

### Deferred `ln-plan` pass — reasoning (volatile)

Decided this session: **do not run `ln-plan` now.** The sync already did the repair half; the decision half is blocked on the KA conversation. Three decisions await his answers:

1. **Close or narrow FE-1107** — whether the residue (`executor.md` conduct realignment + whatever his cards leave open) stays inside FE-1107 or becomes a small new frontier.
2. **His six `memory/cards/executor-*` cards** — if he confirms exhausted → garbage-collect (per-file, literal paths); if genuinely open → they may need frontier representation on his side.
3. **Sequencing the re-braided walkthrough-evidence branch** — the outer-loop evidence FE-1134/FE-1137 still owe stacks after the open branches are re-braided; ordering vs his open work is a joint call.

Natural order: demo/overview session with KA → `ln-plan` with his answers in hand.

### Review findings

None — no `ln-review` ran this session.

### Diagnostic evidence

- **Decision-ID collision**: KA's code (`src/executor/TOPOLOGY.md`, `orchestrate.ts`, `active-tools.ts`) cited `D101-L`/`D102-L`/`I56-L` as executor decisions, but SPEC assigns those IDs to the elicitation scratchpad / neutral seed / scratchpad non-authoritativeness. His FE-1089 sync commit *message* described SPEC rows (D101-L port cutover row, I56-L rewrite, requirement 26, ExecutionPorts lexicon) that **never landed** in `memory/SPEC.md` on `next` — only card deletions + topology edits made it (verified: `git show c382aaa0 -- memory/SPEC.md` diff was empty). Repaired 2026-07-06 by minting D111-L/D112-L/I58-L and updating his files (comment/doc-only edits).
- **KA's six remaining cards read as built, not open**: every acceptance box in all six `executor-*` cards is checked ✓ and the code they name exists (e.g. the pi-tool card ends "Review or tie off FE-1118"). This contradicts LN's "created, not yet built" understanding — **confirm with KA before GC; they are his artifacts.**
- **Live product-conduct drift (not fixed — build work, his seam)**: `src/agents/prompts/executor.md:17` still teaches "`orchestrator_stub` is the honest execution boundary" (pinned by `compose-prompt.test.ts`) while 25 `execute_*` tools incl. the end-to-end run driver are granted. Ownership assigned in PLAN to the re-baselined `orchestrator-tool-port`.

## Decisions and assumptions

| Item | Type | Status | Source |
| ---- | ---- | ------ | ------ |
| D111-L (execute-mode core over injected `ExecutionPorts`), D112-L (run driver / `RunScheduler` seam), I58-L (one declared side effect per lifecycle helper) | decision/invariant | persisted (commit `066e06c3`) | SPEC.md |
| FE-1107 re-baselined: substantially delivered externally, residue only | decision | persisted | PLAN.md §Active |
| Defer `ln-plan` until after KA session | decision | **volatile** (this file only) | conversation |
| KA's six cards presumed exhausted but unconfirmed | assumption | volatile | conversation |
| `docs/DEMO_STACK_OVERVIEW_2026-07-06.md` is a delete-after-use derivative | decision | persisted (noted in the doc) | conversation |

## Repo state

- **Branch**: `ln/fe-1152-refinements` (top of the 7-branch stack over `next`; ahead of origin)
- **Recent commits**: `bd3cdb46` merge-queue docs · `7ce3c232` deferred kick ordering doc · `4871f77f` PR 294 review threads · `bbf4e928` blank review comments · `066e06c3` **the ln-sync + demo doc from this session**
- **Dirty files**: none (worktree clean)
- **Test status**: not run this session (sync edits were doc/comment-only); `npm run check:markdown-links` passed post-edit; no lint errors on touched TS.

## Artifact status

| Artifact | Exists | Current vs conversation |
| --- | --- | --- |
| memory/SPEC.md | yes | current (D111-L/D112-L/I58-L registered) |
| memory/PLAN.md | yes | current for facts; **decision-stale by design** — awaits post-KA `ln-plan` |
| memory/cards/ | 7 files | `walkthrough-batch-2--seed-variants.md` active (Card 3 remains); six `executor-*` cards are KA's — confirm-then-GC, do not delete unilaterally |
| memory/REFACTOR.md | no | n/a |
| docs/DEMO_STACK_OVERVIEW_2026-07-06.md | yes | current; delete after the KA session |

## Next steps

1. **Hold the demo/overview session with KA** using `docs/DEMO_STACK_OVERVIEW_2026-07-06.md` — walk the five-beat arc; settle the four agenda questions (card statuses, FE-1107 close-or-narrow, `executor.md` stub-boundary realignment ownership, shared-file ownership going forward).
2. **Run `ln-plan`** with KA's answers: dispose FE-1107, GC or re-frontier his cards, sequence the re-braided walkthrough-evidence branch (FE-1134/FE-1137 outer-loop evidence).
3. Delete the demo doc and this handoff once absorbed.

## Retirement rule

- Delete or overwrite this file once the post-KA `ln-plan` pass lands and the demo doc is retired.

## Open questions

- Are KA's six `executor-*` cards exhausted (all read as built) or genuinely open?
- FE-1107: close with residue split out, or narrow in place?
- Who owns the `executor.md` conduct realignment (stub-boundary drift) — KA's seam, but flagged from LN's side?
- Where does the re-braided walkthrough-evidence branch stack relative to KA's open work?

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root. It contains the full post-rebase reconciliation state between the LN stack and KA's executor lane.
> The immediate next step is: the demo/overview session with KA (agenda in `docs/DEMO_STACK_OVERVIEW_2026-07-06.md`), then an `ln-plan` pass with his answers.
> If KA has already answered the four open questions, start with `ln-plan`; otherwise prepare/refine the demo doc.
