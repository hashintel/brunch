# Handoff

> Generated by `ln-handoff` at 2026-04-14T11:00:06Z. Read this file to resume work.

## Goal

Carry the post-merge refinement wave forward: use the new walkthrough fixtures and brownfield kickoff grounding to drive the next UI/data-ownership streams cleanly.

## Session State

- **Last completed skill**: `ln-sync` — refreshed `memory/SPEC.md` and `memory/PLAN.md` after merging Agent A (fixtures) and Agent B (brownfield kickoff) so the live docs match the code and the new frontier.
- **Current skill**: `ln-handoff`
- **Flow position**: `grill → spec → plan → [design] → [oracles] → scope → [spike] → build → review → [refactor] → [sync] → handoff`
- **Handoff trigger**: session wrap after merging two parallel streams; the next thread should begin with manual walkthrough evidence-gathering, then pick up the next UI-facing streams.

## In-flight work

> CRITICAL: These artifacts exist only in the prior conversation, not on disk.
> Reproduce them here with full fidelity.

### 1. Immediate pre-build prerequisite — manual fixture walkthrough round

Before starting either of the next implementation streams, do a round of **manual testing based on the database fixture seeding workflow**.

Intent:
- seed realistic states
- walk the UI manually
- capture missing views, weak state transitions, waiting-state ambiguity, and transcript legibility issues
- use that evidence to shape the next scope card(s)

Use the seeded walkthrough workflow in `docs/praxis/manual-testing.md`.

Priority scenarios to inspect first:
- `issue-tracker-kickoff-ready`
- `issue-tracker-design-active`
- `issue-tracker-criteria-ready`
- `issue-tracker-all-phases-closed`

Specific things to watch for during manual testing:
- whether brownfield kickoff now feels grounded enough in a real repo
- where the main chat view still feels under-informative
- what the actual visible waiting states are vs the earlier subjective impression of locking / tearing down
- which phase layouts need stronger differentiation
- whether export-ready / review-ready / in-flight transcript states feel distinct enough

### 2. Immediate pre-build prerequisite — router/query research intake

Before starting the router/query ownership stream, feed in the research the user already compiled about **TanStack Router vs TanStack Query ownership**.

Status:
- this research has **not** yet been integrated into `memory/SPEC.md` or `memory/PLAN.md`
- the next thread should read it before scoping or building the router/query stream

Known on-disk research likely relevant:
- `docs/research/async-server-state-to-ui-sync-for-chat-observer-agents.md`

Conversation-level instruction:
- the router/query ownership theme should be informed by the compiled research first, not improvised from the current implementation alone

### 3. Recommended next two streams — likely safe without worktrees

The next two recommended streams are:
1. **Story-first phase and transcript patterns**
2. **Router/query ownership refinement for interview surfaces**

Conversation conclusion:
- these can probably proceed **without worktrees this time** because they are comparatively independent in file-tree ownership if scoped narrowly
- rationale:
  - story-first work should stay mostly in `src/client/stories/**`
  - router/query ownership should stay in routed client data-loading / invalidation seams

This is a **volatile planning decision** from conversation, not yet encoded elsewhere.

### 4. Recommended sequencing after that pair

After the two streams above are clearer, the next follow-on stream should be:
- **Transcript fidelity and in-flight status surfaces**

Why:
- story work should clarify what states/artifacts should be legible
- router/query work should clarify what data can remain stable through refresh
- transcript fidelity sits at the intersection of those two concerns and is likely to rework if started too early

### 5. Conditional brownfield follow-up

If manual brownfield walkthroughs still show brittle grounding, promote the conditional next item already reflected in `memory/PLAN.md`:
- **Brownfield kickoff typed grounding transport**

Current stance:
- the merged prompt-shaped handoff is good enough for now
- do **not** immediately widen into a typed artifact unless manual walkthrough evidence says the current approach is still too fragile

### 6. Doc-sync state is current but uncommitted

This session updated the live docs to match the merged work:
- `memory/SPEC.md`
- `memory/PLAN.md`

Those edits are in the working tree but **not committed** yet.

### Review findings

> ALL findings from ln-review, not just the one being acted on.

| # | Finding | Status | Implications |
| --- | ------- | ------ | ------------ |
| 1 | Design-prototype components (`app-shell.tsx`, `knowledge-card.tsx`) are only consumed by Ladle stories, not dead production code | `deferred` | Do not "clean up" story-only references under the assumption they are unused. Story/design work should respect them as reference material. |
| 2 | `RouteRoot` naming holdover vs lexicon-preferred `AppLayout` | `deferred` | Cosmetic only; not urgent and should not distract the next streams. |
| 3 | Project index does a redundant API fetch for redirect | `deferred` | Known architectural limitation of the current TanStack Router redirect pattern; unrelated to the immediate next streams unless router/query scoping widens. |
| 4 | `phaseRedirectTargets` duplicates `phaseRouteSegments` | `deferred` | Forced by route type-safety constraints; do not spend time "fixing" this unless the router layer is being reshaped anyway. |
| 5 | Entity card rendering exists in three places (`EntitySidebar`, `GraphView`, dormant `knowledge-card`) | `deferred` | Still a watch item if story/transcript work adds a fourth surface, but not yet a required refactor. |
| 6 | No outer-loop manual browser verification had been done for the recent route/layout slices | `in-progress` | The new walkthrough workspace and manual-testing protocol are the response to this finding, but the actual walkthrough pass still needs to happen before the next implementation streams. |

### Diagnostic evidence

> Concrete proof points that informed diagnoses or shifted direction.
> Without these, a new thread inherits conclusions but not reasoning.

- `src/server/interview.ts`: brownfield kickoff prompt now explicitly says repo understanding is partial, the first `ask_question` call is the durable kickoff handoff, and the `why` field must begin with `Grounding:` for the first question. This is the thin shipped fix.
- `src/server/context.ts`: observer context now includes `Project mode: brownfield`, `Project directory`, and preserves the `Grounding:` handoff text in the current turn context. This is the mechanism by which the observer now sees kickoff grounding.
- `src/server/observer.ts`: observer prompt now has a brownfield kickoff bias; it is explicitly told to use repo-grounded cues from the question and `why` fields as evidence for durable terminology/context/constraints.
- `src/server/context.test.ts` and `src/server/observer.test.ts`: tests assert the brownfield context and `Grounding:` text are present. This is why the merged kickoff improvement was considered sufficient for now without inventing a new transport.
- `src/server/fixtures/scenarios.ts`: `walkthroughScenarioMatrix` now front-loads the walkthrough scenarios in the public seed catalog and names their inspection focus.
- `src/server/fixtures/walkthrough.test.ts`: proves seeded walkthrough scenarios survive reopen/resume and that the export-ready scenario renders expected markdown after re-open. This is the main evidence that the fixture workspace is now a real proving ground rather than ad hoc seeding.
- `docs/praxis/manual-testing.md`: now contains an explicit `BRUNCH_DB` workflow so seed and dev server point at the same DB file. This removes a prior source of "seeded the wrong DB" confusion.
- `src/client/mutations/interview-mutations.ts`: still calls `await router.invalidate()` after submitting a turn response.
- `src/client/routes/project/$id/_view/-interview-controller.ts`: still calls `router.invalidate()` in both `onData` and `onFinish`. This is the main structural evidence behind keeping router/query ownership as an active frontier item.
- `src/client/routes/project/$id/_view/-interview-view.tsx`: still suppresses some assistant artifacts (`data-observer-result`, `data-phase-summary`) and only renders specific part kinds directly. This is a major reason transcript fidelity remains its own next stream.
- Research breadcrumb: `docs/research/async-server-state-to-ui-sync-for-chat-observer-agents.md` is on disk and should be read into the router/query ownership work before scoping/building that stream.

## Decisions and assumptions

| Item | Type | Status | Source |
| ---- | ---- | ------ | ------ |
| The next thread should begin with a manual fixture-seeding walkthrough round before starting either implementation stream | `decision` | `volatile` | conversation |
| The next two best streams are `story-first phase and transcript patterns` and `router/query ownership refinement for interview surfaces` | `decision` | `persisted` | `memory/PLAN.md` + conversation |
| Those two next streams are probably independent enough to run without worktrees if scoped tightly | `assumption` | `volatile` | conversation |
| Transcript fidelity should follow after story-first + router/query, not before | `decision` | `volatile` | conversation |
| Brownfield typed grounding transport is conditional follow-up, not immediate frontier work | `decision` | `persisted` | `memory/PLAN.md` |
| Read-only exploration plus the current prompt-shaped kickoff handoff may be enough for brownfield grounding | `assumption` | `persisted` | `memory/SPEC.md` A47 |
| Fixture-backed walkthrough scenarios are now the outer-loop proving ground for the next UI-facing work | `decision` | `persisted` | `memory/SPEC.md` I103 / `memory/PLAN.md` |
| Router/query ownership work must be informed by the compiled TanStack research before scoping/building | `decision` | `volatile` | conversation |

## Repo state

- **Branch**: `ln/fe-582-e2e-manual-refine-2`
- **Recent commits**:
  - `0a99cb2` — Capture brownfield kickoff follow-up scope
  - `a235151` — Reground brownfield kickoff handoff
  - `6a7e168` — Add fixture-backed walkthrough workspace
  - `43d1082` — remove dangling docs
  - `da9c8b8` — docs: add worktree agent prompts
- **Dirty files**: `memory/SPEC.md`, `memory/PLAN.md`
- **Test status**: `npm run verify` passed in the control worktree after the sync edits (42 files, 324 tests, build green)

## Artifact status

| Artifact | Exists | Current vs conversation |
| -------- | ------ | ----------------------- |
| memory/SPEC.md | yes | current, but uncommitted in working tree |
| memory/PLAN.md | yes | current, but uncommitted in working tree |
| memory/REFACTOR.md | no | n/a |
| HANDOFF.md | yes | current after this write |

## Next steps

1. **Manual testing pass first (no `ln-*` skill yet):** use `docs/praxis/manual-testing.md` and the walkthrough seed scenarios to run a browser walkthrough round, then capture concrete notes about missing views, waiting states, kickoff feel, and transcript legibility.
2. **Research intake + scope router/query:** read the TanStack Router/Query research (at minimum `docs/research/async-server-state-to-ui-sync-for-chat-observer-agents.md` plus any user-supplied notes), then run `ln-scope` for **Router/query ownership refinement for interview surfaces**.
3. **Scope story-first UI lane:** run `ln-scope` for **Story-first phase and transcript patterns**, keeping it local to `src/client/stories/**` and using the manual-testing findings as its evidence base.
4. **Commit the doc sync** (`memory/SPEC.md`, `memory/PLAN.md`) once satisfied with the updated frontier wording.

## Open questions

- Do the seeded manual walkthroughs show that the current brownfield kickoff handoff is good enough, or does it still feel brittle enough to justify typed grounding transport?
- Which waiting states deserve explicit names in the eventual in-flight state vocabulary after a real browser walkthrough?
- Does the story-first stream still look independent enough from the router/query stream once the manual findings are in hand?
- Is there additional TanStack Router/Query research outside `docs/research/async-server-state-to-ui-sync-for-chat-observer-agents.md` that the next thread needs to ingest?

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root for this work area. It contains the full state of in-progress work.
> The immediate next step is: run a manual walkthrough pass using the seeded fixture workflow in `docs/praxis/manual-testing.md`.
> Start by reviewing the In-flight section, running the walkthrough scenarios (`issue-tracker-kickoff-ready`, `issue-tracker-design-active`, `issue-tracker-criteria-ready`, `issue-tracker-all-phases-closed`), and capturing concrete findings before scoping the router/query or story-first streams.
