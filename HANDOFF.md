# Handoff

> Generated by `ln-handoff` at 2026-07-16T16:07:27Z. Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Complete FE-1210 round one: run the held Card 4 live Brunch-vs-Claude comparison witness, then distill the Dora-runnable handover — deadline Friday 2026-07-17 before Lu's two-week leave.

## Session State

- **Last completed skill:** `ln-build` (delegated, coordinator-reviewed) — Cards 1–3 of the tracer scope file landed and were accepted: mission-packet format, frozen actor recipe, judgment prompt pack.
- **Current skill:** `ln-handoff` — in progress until this file is written.
- **Flow position:** plan ✓ → scope ✓ (proof gates, then tracer) → build ✓ (proof gates both held; tracer Cards 1–3) → **Card 4 witness held at user request** → handoff.
- **Handoff trigger:** user chose to hold the Card 4 live witness and wrap up the session; the witness needs an overlay-capable session (pi `interactive_shell`) or a human operator plus 1–2 supervised hours.

## In-flight work

All scope content is persisted in `memory/cards/agent-as-user-comparison--round-one-tracer.md` (Cards 1–3 `done`, Card 4 `next`/held). The genuinely volatile remainder:

1. **Card 4 mission choice — undecided.** The recommended round-one mission is the packet's fictional **library-lockers** worked example (`docs/praxis/comparison-runs/mission-packet.md` §Small fictional worked example); its controller-only reveal key is **not yet instantiated**. Choosing a different mission is legitimate but must be frozen before any lane starts.
2. **Card 4 execution constraint.** Only an overlay-capable coordinator session (pi bridge `interactive_shell`) or a human can run the live lanes. A normal builder subagent must never be delegated Card 4 — the card says so explicitly.
3. **Handover distillation deliberately unscoped.** The Dora entry point + `docs/praxis/comparison-runs.md` update are scoped only after Card 4 evidence exists (anti-speculation gate). Do not pre-author them.
4. **Uncommitted planning state** (see Repo state): the FE-1210 PLAN reconciliation and both scope cards are not yet committed. Committing them is the first mechanical step of any resume.

### Review findings

| # | Finding | Status | Implications |
| --- | --- | --- | --- |
| 1 | Proof-gates Card 2 builder ran file-scoped fix + full default tests + build instead of literal `npm run verify` (to honor file-scoped-fixer constraint) | `addressed` (accepted divergence) | CI's full gate covers the remainder; no action |
| 2 | Cadence gate attempt 1 invalidated by user takeover | `addressed` (retained in evidence note) | Only cost was time; attempt 2 is the valid witness |
| 3 | Cards 1–3 review: commits match declared manifests exactly; `npm run check` re-run independently, green | `addressed` | none |

### Diagnostic evidence

- **Cadence gate (`live-overlay: usable`)** — input delivery ~10 s send→execution; render readback floored by the 60 s `interactive_shell` query rate limit → effective observe-act cycle ≈60–70 s; empty renders possible in the first ~30 s (not target failure). Settings: quietThreshold 3000 / updateInterval 30000 / autoExitOnQuiet false. Evidence (gitignored): `.fixtures/scratch/comparisons/preflight/live-control-cadence.md`.
- **Document-acquisition gate (`entry-point: dev-export (new)`)** — `npm run dev-cli -- document-export --workspace <dir> --spec-id <id> --out <file.md>` (commit `34f4410c`) renders settled graph state via `renderSpecMarkdownOutput`. Witness (gitignored): `.fixtures/scratch/comparisons/preflight/document-acquisition.md` + `settled-spec.md`; seeded workbench `.fixtures/workbenches/document-acquisition-preflight/`.
- Plan-plane rendering (`renderPlanMarkdownOutput`) has **no subcommand** — round one defines "ready" as a specification document only; add a plan export only if a future mission genuinely needs it.

## Decisions and assumptions

| Item | Type | Status | Source |
| --- | --- | --- | --- |
| Pi session is the round-one actor; tui-driver fallback only with capability loss recorded | decision | persisted | PLAN.md §agent-as-user-comparison |
| Brunch + Claude Code required; Cursor best-effort, never blocking | decision | persisted | PLAN.md §agent-as-user-comparison |
| Both proof gates held; verdicts recorded | decision | persisted | memory/cards/…--round-one-proof-gates.md (complete) |
| Library-lockers example as Card 4 mission | assumption (recommended, unconfirmed) | volatile | this handoff |
| Card 4 budget: ~60–70 s per observe-act cycle → 1–2 supervised hours for both lanes + judgment | assumption | volatile | cadence evidence + this session |

## Repo state

- **Branch:** `ln/fe-1210-agent-as-user-comparison` (stacked on `ln/fe-1208-traces-and-evals-1`)
- **Recent commits:** `600dc86b` judgment prompt pack · `e6772e7c` actor recipe · `bdc98015` mission packet · `34f4410c` document-export subcommand
- **Dirty files:** ` M memory/PLAN.md` (FE-1210 activation reconciliation), `?? memory/cards/agent-as-user-comparison--round-one-proof-gates.md` (complete), `?? memory/cards/agent-as-user-comparison--round-one-tracer.md` (active, Card 4 next), this `HANDOFF.md`
- **Test status:** `npm run check` green (re-run at review time); default test suite + build green at commit `34f4410c`

## Artifact status

| Artifact | Exists | Current vs conversation |
| --- | --- | --- |
| memory/SPEC.md | yes | current (untouched this session) |
| memory/PLAN.md | yes | current, uncommitted — FE-1210 active, execution pointer → tracer card |
| memory/cards/agent-as-user-comparison--round-one-proof-gates.md | yes | complete (both gates held); retained as evidence pointer |
| memory/cards/agent-as-user-comparison--round-one-tracer.md | yes | active — Cards 1–3 done, Card 4 next/held |
| memory/REFACTOR.md | no | n/a |

## Next steps

1. Commit the planning state (`memory/PLAN.md`, both scope cards, `HANDOFF.md`) if not already done.
2. Confirm the Card 4 mission (library-lockers recommended), instantiate its controller-only reveal key outside every target cwd, and predeclare the campaign manifest in `.fixtures/scratch/comparisons/<campaign-id>/`.
3. Execute Card 4 in an overlay-capable session or with a human operator, following `.agents/skills/agent-as-user-comparison/SKILL.md` exactly — Brunch lane, then Claude lane, Cursor best-effort.
4. Run the split judgment via `docs/praxis/comparison-runs/judgment-prompt-pack.md`; Dora adjudicates.
5. Promote the reviewed bundle to `.fixtures/runs/agent-as-user-comparison/<campaign-id>/` (`npm run check:promoted-run-paths` before commit), then scope the Dora handover distillation (`ln-scope`) from the actual run evidence.

## Retirement rule

- Delete or overwrite this file once Card 4's witness is executed and its outcome is reconciled into the tracer card and `memory/PLAN.md`, or a newer handoff supersedes it.

## Open questions

- Which mission is frozen for Card 4 — the fictional library-lockers example, or a different one? (Must be settled before any lane starts.)
- Does Dora run Card 4 herself (human operator path) or does an agent session drive it with her adjudicating only the judgment? Affects how much of the actor recipe she needs to internalize before Friday.

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root. FE-1210 round one: Cards 1–3 of `memory/cards/agent-as-user-comparison--round-one-tracer.md` are built and accepted; Card 4 (live Brunch-vs-Claude witness) is held and is the immediate next step.
> Start by confirming the frozen mission (library-lockers recommended), then execute Card 4 per `.agents/skills/agent-as-user-comparison/SKILL.md` in an overlay-capable session — it cannot be delegated to a normal builder subagent.
