# Handoff

> Generated by `ln-handoff` at 2026-06-24. Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Reconcile Brunch's sealed read-only subagent mechanism with the foreground agent model (SPEC D90-L–D93-L, I49-L), and by end-of-branch stand up an **operable `orchestrator` (execute mode) that calls a trivial Node.js stub tool** — a working baseline a colleague extends tomorrow into real task orchestration.

## Session State

- **Last completed skill**: `ln-build` (via builder threads, verified here) — slice 3 (semi-permeable seal) landed and committed `e0c12d6e`.
- **Current skill**: `ln-handoff`.
- **Flow position**: `grill → spec → plan → [design] → [oracles] → scope → [spike] → build → review → [refactor] → [sync]` — looping **scope → build** through the frontier's slice sequence. Slices 1, 2, 3b, 3, 4 done; **slice 5 is next to scope.**
- **Handoff trigger**: capturing state mid-frontier so a fresh thread can continue scope→build on slices 4→5→6 without re-deriving the clarifications and per-slice notes below.

## In-flight work

> Slices 1, 2, 3b, 3 are committed (see Repo state). Their scope cards are on disk under `memory/cards/` (marked `done`, untracked-or-committed). What is volatile is the **re-scoped slice plan 4→6 + the user clarifications that reshaped it**, none of which is on disk yet.

### The frontier slice sequence (re-scoped this session — only partially on disk)

One frontier, one branch (`ln/fe-1054-subagent-reconciliation-ii`, Linear **FE-1054**). The branch was split: groundwork (slices 1–2) lives on `ln/fe-1054-subagent-reconciliation` *below*; this `-ii` branch carries slices 3b→6, **stacked on top of `ln/fe-1058-readiness-bands-interrogation`** (user's deliberate topology — see Decisions; it couples FE-1054-ii to merge after FE-1058).

```
✓ 1   shared-manifest       done (committed below on fe-1054)
✓ 2   composer-core         done (committed below on fe-1054)
✓ 3b  agent-home-unify      done (d6f09736) — card on disk
✓ 3   world-inject-seal     done (e0c12d6e) — card on disk; I29-L flipped here
✓ 4   delegatable-gate      done — card on disk
  5   return-renderer       NOT SCOPED
  6   orchestrator-standup  NOT SCOPED — the end-of-branch readiness target
```

**Build order is strictly sequential — they share files, cannot parallelize.** Overlap test: slices 4+6 both write `runtime-policy.ts`; 3-line of the chain serializes on `subagents/index.ts` + `session.ts`. One builder, one card at a time, in order. Scope each card **just-in-time** (scope N+1 only after N lands) — each slice's reality shifts the next (esp. what 5's `details` payload carries depends on 3's output, already settled; what 6 needs depends on 4).

### Slice 4 — tool resolution + delegatable-set gate (D92-L / I49-L). DONE.

- `src/projections/session/runtime-policy.ts` now populates `elicit.canDelegate` with `explorer`/`researcher`/`projector`/`reviewer` and exports `delegatableAgentsForRuntimeState`.
- `src/app/brunch-tui.ts` injects the active op-mode's delegatable set into `loadBrunchSubagents`; `createBrunchPiExtensions` registers/advertises `subagent` only when that set is non-empty.
- `src/.pi/extensions/subagents/index.ts` advertises and executes only `definitions ∩ delegatableAgents`; a loaded but non-allowlisted agent is refused at execution.
- `src/.pi/extensions/subagents/session.ts` exposes `createSubagentToolCatalog` as the shared source for sovereign manifest-authored grants; `planSubagentTools` resolves against the catalog, not a parent subset.
- Verification landed: oracle 4 in `src/.pi/extensions/subagents/subagents.test.ts` plants a test-only write-capable `worker` and proves `elicit` refuses it; frontmatter cannot author `canDelegate`; `read_graph` resolves as a sovereign child grant. Focused tests + TypeScript + `npm run check` passed. Full Vitest was blocked by the known `better-sqlite3` Node ABI mismatch (compiled 137, current requires 147); no slice assertion failed before the DB-backed failures.

### Slice 5 — return renderer.

- Define the `details` payload (`{ agent, status, text, … }`) and custom `renderCall` / `renderResult` on the `subagent` tool (`src/.pi/extensions/subagents/index.ts`).
- Tool-result `content` is the cross-back into main-agent context; `details` is **render-only, never model context** (already structurally true post-slice-3; this slice adds the renderer).

### Slice 6 — execute mode + orchestrator standup + stub Node tool. THE READINESS TARGET.

- Move `execute` from `PLANNED_OPERATIONAL_MODE_IDS` to live (`src/session/schema/kinds.ts`; `code` already added to planned set in slice 1).
- Add the `orchestrator` foreground manifest to `FOREGROUND_AGENT_ROSTER` (`runtime-policy.ts`) + `src/.pi/agents/orchestrator/SYSTEM.md`.
- Add a **trivial Brunch custom tool** granted to execute's tool policy — "node-based" = a trivial Node.js/CLI thing that does something on the command line, stubbing tomorrow's real task-orchestration code. Later it becomes the thing that *calls workers*; today it just proves the orchestrator can invoke it. (User: custom Brunch tool vs. granting existing `bash` is still open — confirm at scope time; a controlled custom tool reads cleaner for a demo.)
- Prove: launch execute mode, orchestrator calls the stub, it runs.
- **Load-bearing wiring note (from slice 3 verification):** subagent registration is currently **dev-gated behind `context.dev` / `BRUNCH_DEV`** (`src/app/brunch-tui.ts` `const subagents = context.dev ? await loadBrunchSubagents(...)`). Slice 6 must decide: wire execute-mode subagents/tools outside dev-gating, OR run the orchestrator demo under `BRUNCH_DEV`. This is the runnable-path gap.
- **build:pi-assets gotcha:** `package.json` `build:pi-assets` now **hard-enumerates** the six agent dirs (no glob). Adding `orchestrator/` requires editing that line — or switch it to a glob in slice 6.

### Target agent topography (user's sketch — D90-L endpoint)

```
src/.pi/agents/
  elicitor/SYSTEM.md      (fg, elicit — live)
  pi-coder/SYSTEM.md      (fg, code — exists, unwired; PLANNED)
  orchestrator/SYSTEM.md  (fg, execute — slice 6 NEW)
  explorer/SYSTEM.md      (bg — renamed from scout, slice 3b)
  researcher/SYSTEM.md    (bg)
  projector/SYSTEM.md     (bg — renamed from proposer, slice 3b)
  reviewer/SYSTEM.md      (bg — was fg, reclassified slice 3b)
  references/ per agent   (OPEN — mirror agent-skills convention; do NOT scaffold speculatively, add only when an agent has real reference material)
```
Nothing in the directory layout distinguishes fg/bg — only `AgentManifest.kind` / the code-owned registry does (D90-L).

### Oracle battery (persisted in SPEC §Verification Design; build-relevant detail)

1. **Extraction purity (slice 2)** — DONE: COMPOSE goldens byte-identical after composer-core extraction.
2. **Code-owned discovery (slices 1, 3b)** — DONE: planted unlisted `<id>/SYSTEM.md` with write-capable `bash` not spawnable.
3. **Semi-permeable seal (slice 3)** — DONE: assembled prompt carries world snapshot; graph via tool not baked; sibling spec excluded; ambient seal preserved.
4. **Delegatable-set write-safety boundary (slice 4, I49-L)** — PENDING: test-only write-capable manifest proves `elicit` refuses it + frontmatter cannot self-advertise.
- Blind spots (not gated): subagent digest/world-read quality (outer fitness); snapshot-at-spawn staleness (accepted by design).

### Review findings

| # | Finding | Status | Implications |
| --- | --- | --- | --- |
| 1 | Slice 3 oracle-3 isolation test (`injectedWorld` helper) builds a sibling graph but `void`s it — proves "child surfaces parent-scoped reader output, prompt lacks sibling content," does NOT exercise a reader that *could* return the sibling. | `deferred` | Adequate because isolation is **structural** (app-root `graphReadersForSpec(graph, specId)` closes over parent specId; child has no path to a sibling). If a load-bearing standalone test is wanted later, drive both specs through one reader. Not blocking. |
| 2 | `build:pi-assets` hard-enumerates six agent dirs (no glob). | `deferred` | Slice 6 must edit it to add `orchestrator/`, or switch to a glob. Flagged in slice 6 notes above. |

### Diagnostic evidence

- **Slice 1 regression (fixed):** adding `'code'` to `PLANNED_OPERATIONAL_MODE_IDS` flipped the mode-picker note singular→plural; `runtime-axis-picker.test.ts` asserted the old singular string. Fixed to "execute and code are not yet enabled". Caught by full vitest (builder's targeted run missed it).
- **Slice 3 regression (fixed):** extracting `createReadGraphTool` moved `pi.appendEntry` to an **eager** `pi.appendEntry.bind(pi)` at registration; 6 graph-tool tests use a mock `pi` without `appendEntry` → `.bind` threw on undefined. Fixed by guarding: `...(pi.appendEntry ? { appendEntry: pi.appendEntry.bind(pi) } : {})`. The factory already typed `appendEntry` optional + guarded its *use*; only the bind was unguarded. Builder missed it because their sqlite ABI block prevented running DB-backed graph tests.
- **Recurring hazard (3×):** builders run `npm rebuild better-sqlite3` in the shared worktree, compiling for *their* Node ABI and breaking DB tests for other tenants (NODE_MODULE_VERSION 137 vs 147). Each time it forced a verification cycle: `npm rebuild better-sqlite3` locally fixes it (sqlite confirmed OK for node v24.17.0 this session). **Tell builders not to run it in the shared tree, or pin a Node version.** It is a build artifact outside any session manifest — rebuild locally, do not treat as a real regression.

## Decisions and assumptions

| Item | Type | Status | Source |
| --- | --- | --- | --- |
| D90-L shared manifest, code-owned discovery, canDelegate | decision | persisted | SPEC §Decisions |
| D91-L semi-permeable seal (injected world, assembled prompt, snapshot-at-spawn, asymmetric reads) | decision | persisted (Supersedes now active — flipped slice 3) | SPEC §Decisions |
| D92-L sovereign tool grants + op_mode delegatable-set gate | decision | persisted (machinery lands slice 4, not demoed) | SPEC §Decisions |
| D93-L op-mode↔foreground collapse; roster elicit/execute/code; executor→orchestrator | decision | persisted | SPEC §Decisions |
| I29-L flipped to semi-permeable (was sealed-read-only) | invariant | persisted (slice 3) | SPEC Invariant table |
| Operable orchestrator + stub Node tool is an end-of-branch **readiness condition** (expansion of the once-deferred slice 6) | decision | volatile | user, this session |
| scout→explorer, proposer→projector, reviewer fg→bg | decision | persisted (slice 3b) | user; SPEC/code reconciled |
| Stub tool belongs to the **orchestrator directly**, not a worker; no write-capable worker this branch; D92-L capability-inversion demo deferred to tomorrow | decision | volatile | user, this session |
| Slice 4 = build machinery, don't demo (orchestrator canDelegate=[]); oracle 4 is its proof | decision | volatile | user, this session |
| FE-1054-ii stacked **on top of** FE-1058 (couples it to merge after FE-1058) | decision | persisted (gt stack) | user, this session |
| Custom Brunch stub tool vs. granting `bash` to orchestrator | assumption | volatile (resolve at slice-6 scope) | user, this session |

## Repo state

- **Branch**: `ln/fe-1054-subagent-reconciliation-ii` (Linear FE-1054). Working tree clean. `gt ls` shows "needs restack" on this branch and siblings — restack before submit.
- **Stack**: `next` → `ln/fe-1054-subagent-reconciliation` (slices 1–2) → `ln/fe-1058-readiness-bands-interrogation` (D94-L four-band, done) → **`ln/fe-1054-subagent-reconciliation-ii` (HERE, slices 3b–6)** → `ln/fe-xxx-agent-skill-sweep`. The `zzz-*` and worktree branches are other tenants.
- **Recent commits**:
  - `e0c12d6e` FE-1054: semi-permeable subagent seal — inject parent world + assembled prompt (D91-L) ← slice 3
  - `d6f09736` FE-1054: unify agent bodies under src/.pi/agents/<id>/ + renames (D90-L) ← slice 3b
  - `6d99b595` FE-1058: card 2 — I50-L two-carrier guard + four-band closure (readiness-bands, not ours)
  - `87fd6512` FE-xxx: card 1 — four-band derived readiness model (D94-L) (readiness-bands, not ours)
  - `14d5b13a` FE-1054: Reconcile subagents with the foreground agent model (#252) (slices 1–2 squash-merge marker / parent)
- **Test status**: full `npx vitest run` = **1032 pass / 0 fail / 1 skipped** (after local `npm rebuild better-sqlite3`). `npm run check` clean (only 3 pre-existing `unicorn(no-thenable)` warnings in `src/graph/**`, outside our manifest). `tsc -p tsconfig.build.json` clean. `npm run verify` NOT run (it auto-mutates the whole tree via `fix`; co-tenancy constraint — scope fixers to changed files).

## Artifact status

| Artifact | Exists | Current vs conversation |
| --- | --- | --- |
| memory/SPEC.md | yes | current (D90-L–D94-L, I29-L flipped, I49-L/I50-L, oracle battery; slice-3 reconciliation landed) |
| memory/PLAN.md | yes | current through slice 3; **slices 4–6 re-scope + the orchestrator-readiness expansion are NOT yet written into the frontier definition** — reconcile when scoping slice 4 (esp. the "operable orchestrator" acceptance + the build-machinery-not-demo call) |
| memory/cards/ | 5 files | `subagent-reconciliation--{shared-manifest,composer-core,agent-home-unify,world-inject-seal}.md` = done (slices 1,2,3b,3); `readiness-bands-interrogation--four-band-derived-model.md` = not ours (FE-1058). **Slices 4,5,6 cards do not exist yet.** |
| memory/REFACTOR.md | no | n/a |

## Next steps

1. **`ln-scope` → `ln-build`** slice 5 (return-renderer).
2. **`ln-scope` → `ln-build`** slice 6 (orchestrator-standup) — the readiness target. Resolve the dev-gating runnable-path question + the custom-tool-vs-bash question at scope time; edit `build:pi-assets` for `orchestrator/`.
3. Before submit: `gt restack` (branch shows "needs restack"); confirm FE-1058 lands first (we're stacked on it).

## Retirement rule

- Delete or overwrite this file once slices 4–6 are scoped + built and the volatile clarifications above are absorbed into `memory/PLAN.md` + code, or superseded by a newer `HANDOFF.md`.

## Open questions

- **Slice 6 stub tool shape** — custom Brunch tool (cleaner for a controlled demo) vs. granting existing `bash` to the orchestrator. Resolve at slice-6 scope.
- **Slice 6 runnable path** — wire execute-mode subagents/tools outside `context.dev`/`BRUNCH_DEV`, or run the orchestrator demo under `BRUNCH_DEV`? The seal wiring is currently dev-gated.
- **FE-1058 merge ordering** — FE-1054-ii is stacked on FE-1058, so the subagent capability work can only merge after readiness-bands. Confirmed intentional this session; flag if FE-1058 hits review friction.

## Resume prompt

> Read `HANDOFF.md` in the workspace root. It holds the live state of the `subagent-reconciliation` frontier (Linear FE-1054, branch `ln/fe-1054-subagent-reconciliation-ii`). Design/plan/oracles are persisted in `memory/SPEC.md` (D90-L–D93-L, I49-L) and `memory/PLAN.md`. Slices 1, 2, 3b, 3 are built and committed (through `e0c12d6e`); slice 4 (delegatable-set gate) is built in the current worktree. The immediate next step is `ln-scope` on **slice 5** (return renderer), then scope→build slice 6 (execute mode + `orchestrator` standup + a trivial Node.js stub tool the orchestrator calls — the end-of-branch readiness target for a colleague tomorrow). Build order is strictly sequential (shared files). Before full DB-backed verification, the local `better-sqlite3` native module must match the active Node ABI; the current full Vitest run failed for compiled 137 vs required 147. Do not run `npm rebuild` for a foreign Node ABI in the shared worktree.
