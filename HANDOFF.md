# Handoff

> Generated by `ln-handoff` at 2026-06-12 (post-walkthrough). Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Land the demo lane's lower line on `ln/fe-852-below-the-line` — the session-startup beat (seed → kick → assistant-authored gap-grounded opening) is now **complete and live-verified**; next is quality review + canonical sync before block 3 (`generalized-capture`).

## Session State

- **Last completed skill**: `ln-build` — `origination-native-elicitation` 3-card chain done (commits `f9f4bee8`, `af31d728`, `54905d30`/`89ca6d9e`); before that, `origination-kick-live` chain, provider-legality fix, seed `--reset` widening, system-prompt-mirror fix, and the FE-852/FE-857 frontiers — all on this branch.
- **Current skill**: `ln-handoff`.
- **Flow position**: build → **review → sync** (user explicitly wants `ln-review` then `ln-sync` in the fresh thread).
- **Handoff trigger**: session end after successful manual walkthrough; fresh thread for review/sync.

## In-flight work

### 1. Walkthrough verdict (2026-06-12, NOT yet recorded in PLAN — mini-sync trigger)

User ran the full walkthrough after the chain landed and reports it **working as expected**: fresh seeded workbench → boot → no canned offer block → assistant-authored opening. This closes the outer-loop residual that PLAN still describes as pending:

- `memory/PLAN.md` §Recently Completed, `origination-native-elicitation` entry ends "**opening quality pending next walkthrough**" — now stale; the walkthrough passed. `ln-sync` should record the pass (date 2026-06-12, alpha-grounding workbench).

### 2. ln-induct candidate: "fixture-validated shape vs. real provider" fault family (chat-only)

Three confirmed instances of the same latent fault class this session, all caught only by live runs:

1. Anthropic `tool_use_id` charset (`exchangeId:toolName` → 400) — faux provider validates nothing.
2. Orphan `tool_result` without paired `tool_use` (would have been the next 400) — fixed via synthetic call+result pairs.
3. `system-prompt.md` mirror silently writing nothing — Anthropic serializes `system` as array-of-blocks; the unit test fed a string.

A fourth adjacent instance from earlier: harness-as-false-proof (Tier-2 oracles drove the turn the product never triggered). The lesson is partially recorded (PLAN 2026-06-12 follow-on note; `src/session/README.md` provider-legality rule), but the **systematic audit is not done**: recommend `ln-induct` with this fault type during the review pass — survey for other shapes/contracts validated only against hand-built fixtures (candidate areas: exchange details schemas vs. real tool events, pi event payload shapes in extensions, provider payload assumptions in introspection/compaction).

### 3. Silent partial-edit failure — PLAN coherence check needed

During this session one multi-edit batch to `memory/PLAN.md` failed validation and was only partially re-applied; the gap (missing frontier definition/sequencing entry for `origination-native-elicitation`) was discovered and repaired at chain close (`54905d30`). **Review/sync should sanity-check PLAN structural coherence** (every Active/Recently-Completed frontier has a definition; dependency nodes match sequencing) in case other batch edits were silently dropped earlier.

### 4. Named-but-unbuilt options (deliberately deferred, confirm during sync they're visible enough)

- **Debug mirror widening**: `brunch.kick` + the (now retired) offer pair are/were not mirrored to `entry-contents.md` (pi-side appends). User never requested it; with the canned offer gone the gap is smaller. Status: optional, chat-only.
- **"Ultra compact" graph-overview renderer variant** for very large graph seeds — recorded in SPEC D78-L + PLAN frontier (persisted; listed here only as review context).
- **`docs/praxis/dev-server-logs.md` is stale** (describes a vite/api dev-server world predating the TUI flow) — flagged sessions ago, still untouched. Good `ln-sync` fodder.
- **`dev-seed-fixtures` ◐ rows** (explicit all-seeds flag; seed disposition catalog) — persisted in PLAN, unchanged.
- **Tripwire** (persisted in PLAN): foreign `label` entries at the transcript tail read as non-debt leaves in `latestTailOwesAssistant` — harden only if `/tree` labeling becomes a Brunch flow.

### 5. Branch/stack housekeeping (volatile)

- `ln/fe-852-below-the-line` now carries: FE-848 seed-DX prep + plan reshape, FE-852 (gap affordances, D78/D49 revision, origination-native-elicitation), FE-857 (carrier migration, seed payload, kick-live, provider legality, debug mirrors), judo pass. **Not yet submitted** (`gt submit` pending; PR title per convention would be `FE-852: …`).
- `ln/fe-858-above-the-line` exists in the **brunch-next-omega worktree**, parented off `next`, flagged *needs restack* — it predates ~15 commits here. Restack after submit, per the two-branch model (above-line stacks on below-line). Co-tenancy: that worktree is not this session's to mutate.
- Linear: FE-857 exists (block 2 + its follow-ons rode it); blocks 2¾ rode FE-852 by execution; block 3 (`generalized-capture`) and `demo-polish` still need FE issues on start.

### Review findings

No formal `ln-review` ran this session. Known review-relevant context for the fresh thread:

| # | Item | Status | Implications |
| --- | --- | --- | --- |
| 1 | Prior judo pass (`c16fbb71`) collapsed origination choreography; seam has since grown again (kick trigger, workspace threading, retirement) | n/a | `ln-review` should re-audit `src/session/originate-assistant-turn.ts` + `brunch-tui.ts` factory tail |
| 2 | `tier-2-harness.ts` has grown 4 boot modes + faux-services helper this session | n/a | check for extraction pressure / duplicated env save-restore blocks |
| 3 | `structured-exchange-loop.ts` retains response-side synthetic pair machinery while present-side minting moved to probe-land | n/a | check residue: anything now probe-only still exported from product paths? |

### Diagnostic evidence

- TUI screenshot + transcript (2026-06-12 morning walkthrough): canned offer block rendered with parity-era copy; model called `read_graph` despite seed; no kick UI for answers → drove the D78-L/D49-L revision.
- Session JSONL error entry: `400 … tool_use_id: String should match pattern '^[a-zA-Z0-9_-]+$'` on `messages.1.content.0.tool_result` → proved both the charset and orphan-tool_result defects.
- `pi-mono` source: Anthropic `params.system` is always an array of text blocks (`packages/ai/src/providers/anthropic.ts:910–929`) → proved the mirror shape bug.
- `grep`: zero non-test `session.prompt`/`triggerTurn` call sites → proved harness-as-false-proof for the kick.
- pi `session-manager.ts:128`: `display: false` = "hidden entirely" (TUI only); content still enters LLM context — settled the invisible-entries UX question.

## Decisions and assumptions

| Item | Type | Status | Source |
| --- | --- | --- | --- |
| D78-L revised: content-rich seed + assistant-authored opening; canned exchange superseded | decision | persisted | SPEC.md |
| D49-L revised: `triggerExchange` = kick surface | decision | persisted | SPEC.md |
| API rule: reconciler seam → `appendCustomMessageEntry`; out-of-band → `pi.sendMessage`/`sendCustomMessage` | decision | persisted | PLAN + session README |
| Provider-legality rule: synthetic exchange tuples = paired call+result, `__` ids | decision | persisted | session README + PLAN |
| setLabel: don't label continuity entries | decision | persisted | PLAN frontier note |
| Walkthrough pass 2026-06-12 (native opening works) | evidence | **volatile — this file only** | conversation |
| ln-induct on fixture-vs-real fault family recommended | proposal | **volatile — this file only** | conversation |

## Repo state

- **Branch**: `ln/fe-852-below-the-line` (clean; parent `next`; not submitted)
- **Recent commits**: `89ca6d9e` retire chain · `54905d30` elicitor guidance + frontier done · `af31d728` content-rich seed · `f9f4bee8` retire canned exchange · `3117df8d` D78/D49 revision · `8905af3b` system-prompt mirror fix · `f47c662b` provider-legal tuples · `01c05c36` seed --reset widening
- **Dirty files**: none
- **Test status**: full `npm run verify` green at `89ca6d9e` (869 passed, 1 todo; build clean)

## Artifact status

| Artifact | Exists | Current vs conversation |
| --- | --- | --- |
| memory/SPEC.md | yes | current (D78-L/D49-L/I46-L revised; I45–I47 coverage cells accurate) |
| memory/PLAN.md | yes | current **except** the stale "opening quality pending next walkthrough" line (see In-flight #1); structural coherence check advised (#3) |
| memory/cards/ | `renderer-golden-coverage--render-stage-chain.md` | active but **deferred below the demo line** (alpha-week); not next work |
| memory/REFACTOR.md | no | n/a |

## Next steps

1. **`ln-review`** — audit the burst (≈15 commits across 5 frontiers): origination seam + factory tail, tier-2 harness growth, structured-exchange residue after the probe-land relocation, debug-cache surfaces, seed CLI. Fold in the **`ln-induct`** pass on the fixture-vs-real fault family (In-flight #2).
2. **`ln-sync`** — record the walkthrough pass (In-flight #1), PLAN coherence check (#3), retire `docs/praxis/dev-server-logs.md` staleness or card it, archive older Recently Completed entries to PLAN_HISTORY.
3. Then either tie off (`gt submit` + restack `fe-858-above-the-line` in omega) or open the **`generalized-capture` grill prelude** (block 3) — user's call.

## Retirement rule

- Delete or overwrite this file once the volatile state above is absorbed into `memory/SPEC.md`, `memory/PLAN.md`, code, or a newer `HANDOFF.md`.

## Open questions

- Should the `entry-contents.md` mirror widen to pi-side appends (`brunch.kick`)? (optional; user never confirmed)
- Block 3 + `demo-polish` Linear issues: create at start, per plan — titles needed.
- Does the walkthrough's opening quality suggest any elicitor-guidance tuning before block 3, or is it good enough to build capture on? (User said "working as expected" — no defects named.)

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root for this work area. It contains the full state of in-progress work.
> The immediate next step is: `ln-review` over the ~15-commit burst on `ln/fe-852-below-the-line` (origination seam, tier-2 harness, structured-exchange residue, debug cache, seed CLI), folding in an `ln-induct` pass on the "fixture-validated shape vs. real provider" fault family described in In-flight #2.
> Start by reading the In-flight section, then run `ln-review`; follow with `ln-sync` to absorb In-flight #1 and #3.
