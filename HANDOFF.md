# Handoff

> Generated by `ln-handoff` at 2026-04-21 13:13 local. Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Finish Track B frontier item 3 by building the remaining two scoped cards in series: first the markdown export projection cleanup, then the output-route preview polish.

## Session State

- **Last completed skill**: `ln-build` — implemented Track B Card 1 so completed specifications now land on the output route from the specification index redirect, with regression coverage.
- **Current skill**: `ln-handoff`
- **Flow position**: `grill → spec → plan → [design] → [oracles] → scope → [spike] → build → review → [refactor] → [sync]`
- **Handoff trigger**: resume work in a fresh thread and have the next agent build the remaining two Track B cards in serial without revisiting planning.

## In-flight work

> CRITICAL: These artifacts exist only in the prior conversation, not on disk.
> Reproduce them here with full fidelity.

### Serial-build directive

- Stay on the current branch; do not create Linear issues or Graphite branches for this continuation.
- Use `memory/CARDS_TRACK_B.md` as the live queue for Track B. Do not touch `memory/CARDS.md`; it is owned by the parallel Track A work.
- Build the remaining cards in this order:
  1. Card 2 — Markdown export foregrounds accepted outputs
  2. Card 3 — Output preview becomes the readable completion surface
- Keep them on the same branch and commit each completed card-sized slice.
- Run the repo verification harness after each card: inner loop `npm run fix`, then gate `npm run verify`.
- Reconcile `memory/CARDS_TRACK_B.md` after each card by advancing status markers.

### Queue validity note

- Card 2 and Card 3 were scoped as sequentially obvious follow-through for the same frontier item.
- Card 3 should still follow Card 2 because the preview copy/presentation should reflect the final markdown/document shape rather than race ahead of it.

## Review findings

| # | Finding | Status | Implications |
| --- | --- | --- | --- |
| 1 | No `ln-review` findings were produced in this session. | `n/a` | The next thread can continue with `ln-build` directly. |

## Diagnostic evidence

- [src/client/routes/specification/$id/index.tsx](file:///Users/lunelson/Code/hashintel/brunch/src/client/routes/specification/%24id/index.tsx#L11-L33) previously redirected `/specification/$id/` to the current phase even when all phases were closed; this is why Card 1 existed and why output-route completion semantics mattered.
- [src/client/__tests__/router.test.tsx](file:///Users/lunelson/Code/hashintel/brunch/src/client/__tests__/router.test.tsx#L235-L269) now proves both redirect paths: incomplete specifications still land on grounding, completed specifications land on `/export`.
- [memory/CARDS_TRACK_B.md](file:///Users/lunelson/Code/hashintel/brunch/memory/CARDS_TRACK_B.md#L29-L55) is the authoritative queued scope for the remaining two Track B cards.

## Decisions and assumptions

| Item | Type | Status | Source |
| ---- | ---- | ------ | ------ |
| Build Track B Cards 2 and 3 in serial on the same branch. | `decision` | `volatile` | conversation |
| `memory/CARDS_TRACK_B.md` is the Track B execution queue because `memory/CARDS.md` is reserved for Track A. | `decision` | `persisted` | conversation + `memory/CARDS_TRACK_B.md` |
| Card 3 should follow Card 2 so output-page presentation reflects the settled markdown/export shape. | `assumption` | `volatile` | conversation |

## Repo state

- **Branch**: `ln/fe-610-revise-restrict-reduce-iii`
- **Recent commits**:
  - `3378476` — Route completed specs to output
  - `a51d609` — chore: mark Track A #2 (homepage workspace binding) done in PLAN.md
  - `6f213b0` — feat: surface workspace (CWD) identity on the homepage
- **Dirty files**: no uncommitted Track B code changes at handoff time
- **Test status**: `npm run verify` passed after Card 1 (`check`, `test`, and `build` all green)

## Artifact status

| Artifact | Exists | Current vs conversation |
| ------------------ | ------ | ------------------------- |
| memory/SPEC.md | yes | current |
| memory/PLAN.md | yes | current |
| memory/CARDS.md | yes | current for Track A / n-a for this handoff |
| memory/REFACTOR.md | no | n/a |

## Next steps

1. Run `ln-build` on Card 2 from [memory/CARDS_TRACK_B.md](file:///Users/lunelson/Code/hashintel/brunch/memory/CARDS_TRACK_B.md#L29-L41): make the markdown export foreground accepted `Requirements` and `Acceptance Criteria`, improve supporting-section ordering, and make caveat wording human-readable.
2. Run `npm run fix` and `npm run verify`, then commit the Card 2 slice and mark Card 2 done in [memory/CARDS_TRACK_B.md](file:///Users/lunelson/Code/hashintel/brunch/memory/CARDS_TRACK_B.md#L29-L41).
3. Run `ln-build` on Card 3 from [memory/CARDS_TRACK_B.md](file:///Users/lunelson/Code/hashintel/brunch/memory/CARDS_TRACK_B.md#L43-L55), verify again, and commit that slice.

## Retirement rule

- Delete or overwrite this file once the volatile state above is absorbed into `memory/SPEC.md`, `memory/PLAN.md`, code, or a newer `HANDOFF.md`.

## Open questions

- None currently. If Card 2 reveals that the export projection needs a durable spec/plan change rather than bounded cleanup, pause and route back through `ln-scope` instead of widening the slice silently.

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root for this work area. It contains the full state of in-progress Track B work.
> The immediate next step is: run `ln-build` on Card 2 in `memory/CARDS_TRACK_B.md`, then Card 3 immediately after if Card 2 verifies cleanly.
> Stay on the current branch, do not touch `memory/CARDS.md`, use `memory/CARDS_TRACK_B.md` as the Track B queue, and run `npm run fix` plus `npm run verify` after each card before committing.
