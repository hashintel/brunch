# Handoff

> Generated by `ln-handoff` at 2026-05-15. Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

FE-716 / `chat-runtime-secondary-chats` — render side, reconciliation, qa, strategy chats inline as collapsible secondary chats over the existing chat/turn substrate (no `thread` table). V1 narrowing is in effect: surface every V3.1 side-chat behavior through the elevated unified-workspace shape, then retire `SideChatPopover`.

## Session State

- **Last completed skill**: `ln-build` — finished cards C3c-mount, C3c-wire, and C4 of the FE-716 V1 queue in this session.
- **Current skill**: `ln-handoff` (capturing state before context rolls over or branch finalization).
- **Flow position**: `grill → spec → plan → [design] → [oracles] → scope → [spike] → build → review → [refactor] → [sync]` — sitting on **build**, mid-queue (cards C5–C10 still ahead).
- **Handoff trigger**: end-of-session capture before next slice (C5) and before the uncommitted C3c-mount + C3c-wire + C4 work is split into graphite commits.

## In-flight work

> CRITICAL: These artifacts exist only in the prior conversation, not on disk.
> Reproduce them here with full fidelity.

### Uncommitted but completed (this session)

Three cards are **done in working tree**, fully verified, but **not yet committed** as separate graphite commits matching the existing FE-716 sequence (C0…C3c-route are each their own commit).

#### C3c-mount — done (uncommitted)

- **What landed**:
  - `useContinuousWorkspaceController` projects `specificationState.secondaryChats` into `secondaryChatsByInvokedTurnId: ReadonlyMap<number, readonly SecondaryChatState[]>` and exposes it on `ContinuousWorkspaceController`.
  - `ContinuousWorkspaceView` threads it into `WorkspaceTranscriptArtifacts`.
  - `WorkspaceTranscriptArtifacts` adds `getArtifactAnchorTurnId(artifact)` covering `answered-turn`, `prefaced-question`, `answered-review-turn`, `answered-revision-review`, `collapsed-review-turn`, `accepted-closure`, `persisted-turn`, `active-prefaced-question`, `phase-summary`. Renders `<SecondaryChatCollapsible>` instances inside `data-testid="secondary-chats-for-turn-{id}"` slot beneath each matching artifact. No-orphan: chats whose `invoked_in_turn_id` doesn't match a rendered artifact never render.
- **Tests added**:
  - `src/client/routes/specification/$id/_view/__tests__/-workspace-transcript-artifacts.test.tsx` — 4 tests (anchor matching, collapsed-by-default, no-orphan, multi-chat per turn).
  - `-continuous-workspace-view.test.tsx` — added prop-threading test asserting controller's `secondaryChatsByInvokedTurnId` reaches the artifacts renderer by reference.

#### C3c-wire — done (uncommitted)

- **What landed**:
  - `specificationSchema` exposes `primary_chat_id: z.number().int().positive().nullable().optional()`. Optional was chosen for transition (avoids touching ~14 spec fixtures); production data always has it.
  - New file `src/client/components/secondary-chat-trigger.tsx` exports:
    - `useCreateSecondaryChatMutation(specificationId)` — POSTs to `/api/specifications/:id/secondary-chats`, invalidates bundle on success.
    - `SecondaryChatTriggerProvider` + `useSecondaryChatTrigger()` — derives `parentChatId` (= `primary_chat_id`) and `invokedInTurnId` (= `active_turn_id`) from the spec bundle; exposes `create({ kind, id })` callback.
  - Provider mounted in `route.tsx` alongside `SideChatHost`.
  - `ItemActionRail` in `-structured-list-view.tsx` gained an `Open inline chat` button (`data-graph-action="open-inline-chat"`, `MessagesSquare` icon) sitting alongside the existing `chat-with` popover trigger. Disabled when context unavailable or while pending.
- **Tests added**: `secondary-chat-trigger.test.tsx` — 5 tests (canCreate true/false branches, POST payload, invalidation, no-POST when canCreate=false).

#### C4 — done (uncommitted)

- **What landed**:
  - Migration `drizzle/0021_chat_mode.sql`: `ALTER TABLE chat ADD mode text;` Journal updated (idx 21, tag `0021_chat_mode`).
  - `schema.ts` chat table: `mode: text({ enum: ['explore', 'edit'] })` (nullable).
  - `createSecondaryChat` defaults to `'explore'`; new `setSecondaryChatMode(db, chatId, mode)` rejects non-secondary chats (parent_chat_id is null) and missing chats.
  - `db.ts` exports `setSecondaryChatMode` + type `SecondaryChatMode`.
  - `secondaryChatStateSchema.chat.mode: secondaryChatModeSchema.nullable()` propagates through bundle. `core.ts` projection includes mode.
  - New PATCH route `/api/specifications/:id/secondary-chats/:chatId/mode` — `handleSetSecondaryChatModeRequest`.
  - Client: `useSetSecondaryChatModeMutation(specificationId, chatId)` + inline `patchJsonMutation` helper in `secondary-chat-trigger.tsx`. UI toggle (Ask = explore, Edit = edit) added to `SecondaryChatCollapsible` as a sibling to the trigger (NOT nested — earlier nested-button warning was fixed). Toggle is read-only when no `onSetMode` provided. `WorkspaceTranscriptArtifacts` wraps via thin `SecondaryChatCollapsibleWithMode` for the mutation hook.
- **Tests added**:
  - `chat-substrate.test.ts` — default mode='explore', explicit mode='edit', `setSecondaryChatMode` updates + invariants (rejects primary chat, missing chat).
  - `app.test.ts` — PATCH happy path with bundle round-trip, 400 on invalid mode, 404 on cross-spec chatId, 404 when targeting primary interview chat.
  - `secondary-chat-collapsible.test.tsx` — toggle reflects persisted mode, falls back to explore when null, click invokes `onSetMode`, no-op when clicking active mode, disabled while pending or read-only.
- **Out-of-scope (deferred to C5)**: actual streaming-with-tools wiring for secondary chats; `getSideChatTools(chat.mode)` is unchanged but no streaming endpoint consumes it for secondary chats yet.

### Next card queue (live from `memory/CARDS.md`)

C0, C1, C2, C3a, C3b, C3c-route, C3c-mount, C3c-wire, C4 — all **done**.

Next card: **C5 — In-thread patch staging on secondary chats** (per CARDS.md):

- **What:** Port #138's in-thread staged-patches strip onto the chat substrate. Patches stay turn artifacts; accepted mutations still flow through Brunch-owned handlers (no new source of semantic truth).
- **Why sixth:** Closes the Edit-mode loop end-to-end.
- **Verification:** staging/apply/cancel tests on a secondary chat; regression on the V3.1 side-chat edit flow.
- **Harvest:** PR #138's in-thread staging UI; `pendingPatches` plumbing.

After C5: C6 (`#` knowledge-item symbol injection — V1 surface only), C7 (agent-run inline rendering + chat.kind decision; default posture: keep enum, project from `first_turn_role='system'`), C8 (`SideChatPopover` retirement), C9 (lightweight reconciliation-element view), C10 (V1 closure + frontier closeout).

### Review findings

> ALL findings from ln-review, not just the one being acted on.

| #   | Finding | Status | Implications |
| --- | ------- | ------ | ------------ |
| —   | No `ln-review` was run this session. | n/a | C5–C10 still ahead before frontier closeout (C10) which schedules `npm run verify` + manual walkthrough; no formal code review yet. |

### Diagnostic evidence

> Concrete proof points that informed diagnoses or shifted direction.

- Two transient `npm run verify` failures with `ENOSPC: no space left on device` on `/var/folders/26/.../T/...` (disk at 100%, ~200Mi free). Root cause: macOS dev disk pressure, not test/code. Each affected suite (`use-simple-store.test.ts`, `capabilities.test.ts`) passed when re-run in isolation. **Implication for next thread**: if `npm run verify` flakes, retry the failed suite before treating it as a real regression.
- Initial trigger-context test (`secondary-chat-trigger.test.tsx > POST happy path`) failed with "expected 1 call, got 2" because invalidating the bundle triggers a refetch which calls `fetch` again. Loosened assertion to `toHaveBeenCalled()` and verified payload via `mock.calls[0]` — bundle invalidation is the second fetch.
- `bundle:` queryKey uses singular `'specification'`, not `'specifications'` (verified at `src/client/routes/specification/$id/-specification-data.ts:12`). The trigger test's invalidation assertion was corrected from plural to singular.
- React warning: "<button> cannot contain a nested <button>" was raised when I first nested the Ask/Edit toggle inside `CollapsibleTrigger`. Fixed by promoting toggle to a sibling inside a flex container, then removed the now-unnecessary `event.stopPropagation()`.
- `primary_chat_id` is non-null in production once a spec exists (set during `createSpecification`'s transactional insert) but the schema field had to be `.nullable().optional()` because adding a required field to `specificationSchema` broke ~14 test fixtures with TS errors. Optional was chosen as the smallest-churn migration; revisit if/when fixtures are regenerated.
- Drizzle migrations are auto-applied on `createDb()` (no explicit `npm run migrate`); `_journal.json` must be edited alongside each new SQL file. Followed pattern from migration 0020.

## Decisions and assumptions

| Item | Type | Status | Source |
| ---- | ---- | ------ | ------ |
| `mode` stored as nullable column on `chat` (smallest viable storage; not a separate `chat_strategy_state` table) | decision | persisted | `src/server/schema.ts`, migration `drizzle/0021_chat_mode.sql` |
| `'explore'` is the default mode for newly created secondary chats | decision | persisted | `createSecondaryChat` in `src/server/db/specification-store.ts` |
| `setSecondaryChatMode` rejects primary/interview chats (parent_chat_id IS NULL) | decision | persisted | helper + tests in `chat-substrate.test.ts` |
| `primary_chat_id` exposed on client `specificationSchema` as `.nullable().optional()` (transition-friendly) | decision | persisted | `src/shared/api-types.ts`; revisit if all spec test fixtures get regenerated |
| `parentChatId` for the V1 trigger always = the spec's primary chat (no tertiary chat support) | assumption | volatile | `SecondaryChatTriggerProvider`; future work may pass arbitrary parent chat ids |
| `invokedInTurnId` for the V1 trigger always = `specification.active_turn_id` | assumption | volatile | `SecondaryChatTriggerProvider`; reasonable because the user is on the workspace transcript |
| Mode toggle is rendered as a sibling to `CollapsibleTrigger` (not nested) to avoid nested `<button>` | decision | persisted | `SecondaryChatCollapsible` |
| `WorkspaceTranscriptArtifacts` wraps `SecondaryChatCollapsible` in `SecondaryChatCollapsibleWithMode` so the test file's mock-router environment can inject mode wiring without breaking the standalone component | decision | persisted | `-workspace-transcript-artifacts.tsx`; `-workspace-transcript-artifacts.test.tsx` mocks `secondary-chat-trigger` |
| C5+ defers actual mode-aware streaming for secondary chats (no streaming endpoint exists yet for them); C4 lands persistence + UI toggle only | decision | persisted in CARDS.md C4 "Out of scope" | `memory/CARDS.md` |
| Agent-run secondary chats (C7): default posture is keep `chat.kind` enum at `interview | side_chat`, project agent-run from `first_turn_role='system'` (per HANDOFF carry-forward) | assumption | volatile | unresolved until C7 |

## Repo state

- **Branch**: `ka/fe-716-chat-runtime-unified-secondary-chats` (stacked on `ln/fe-709-reconciliations` / PR #139, awaiting merge to main).
- **Recent commits** (last 8):
  - `724653c0` FE-716: C3c-route — POST /api/specifications/:id/secondary-chats
  - `e8ee08eb` FE-716: C3b — Add SecondaryChatCollapsible standalone component
  - `033913fc` FE-716: C3a — Surface secondary chats in the SpecificationState bundle
  - `ca40dcfe` FE-716: C2 — Add createSecondaryChat and createKickoffTurn helpers
  - `8c3eda7b` FE-716: C1 — Add secondary-chat substrate columns to chat table
  - `2fa7b1ed` FE-716: C0 — Bring forward UNIFIED_CHAT_UX.md design brief
  - `a319b107` FE-716: Plan inline-secondary-chats frontier with V1 card queue
  - `6b73a7cf` align and update convo runtime doc with cross-references
- **Dirty files** (uncommitted; spans C3c-mount + C3c-wire + C4):
  - Modified: `drizzle/meta/_journal.json`, `memory/CARDS.md`, `src/client/components/__tests__/secondary-chat-collapsible.test.tsx`, `src/client/components/secondary-chat-collapsible.tsx`, `src/client/routes/specification/$id/-structured-list-view.tsx`, `src/client/routes/specification/$id/_view/-continuous-workspace-controller.ts`, `src/client/routes/specification/$id/_view/-continuous-workspace-view.tsx`, `src/client/routes/specification/$id/_view/-workspace-transcript-artifacts.tsx`, `src/client/routes/specification/$id/_view/__tests__/-continuous-workspace-view.test.tsx`, `src/client/routes/specification/$id/route.tsx`, `src/server/app.test.ts`, `src/server/app.ts`, `src/server/chat-substrate.test.ts`, `src/server/core.ts`, `src/server/db.ts`, `src/server/db/specification-store.ts`, `src/server/schema.ts`, `src/server/secondary-chat-route.ts`, `src/shared/api-types.ts`
  - Added (untracked): `drizzle/0021_chat_mode.sql`, `src/client/components/__tests__/secondary-chat-trigger.test.tsx`, `src/client/components/secondary-chat-trigger.tsx`, `src/client/routes/specification/$id/_view/__tests__/-workspace-transcript-artifacts.test.tsx`
- **Test status**: `npm run verify` last green at end of C4 — **103 test files / 1317 tests passed; build clean**. (Earlier intermediate runs flaked once each on disk-pressure ENOSPC; both passed in isolation.)

## Artifact status

| Artifact | Exists | Current vs conversation |
| -------- | ------ | ----------------------- |
| memory/SPEC.md | yes | current — no SPEC-level changes this session (no decisions promoted to A-numbered assumptions). |
| memory/PLAN.md | yes | current — frontier `chat-runtime-secondary-chats` already marked Active with V1 narrowing; no PLAN edits this session. |
| memory/CARDS.md | yes | **stale relative to commits** — CARDS.md is updated to mark C3c-mount, C3c-wire, and C4 as done, but those edits are uncommitted alongside the code. Not stale relative to conversation. |
| memory/REFACTOR.md | no | n/a — no refactor in flight. |

## Next steps

1. **Commit current work into the FE-716 graphite stack.** The existing pattern is one commit per card (C0…C3c-route each have their own commit). Split the dirty/added files into three commits matching the cards:
   - `FE-716: C3c-mount — Render secondary chats inline under their anchor turn`
     - Files: `-continuous-workspace-controller.ts`, `-continuous-workspace-view.tsx`, `-workspace-transcript-artifacts.tsx` (the anchor + wrapper bits, but NOT the mode wrapper — see split notes), `-continuous-workspace-view.test.tsx`, new `-workspace-transcript-artifacts.test.tsx`.
   - `FE-716: C3c-wire — useCreateSecondaryChat trigger + Open inline chat button`
     - Files: `src/shared/api-types.ts` (`primary_chat_id` field), new `src/client/components/secondary-chat-trigger.tsx` (only the create-side at this point — see split notes), new `secondary-chat-trigger.test.tsx`, `route.tsx`, `-structured-list-view.tsx`.
   - `FE-716: C4 — Persist Ask/Edit mode on secondary chats with toggle UI`
     - Files: `drizzle/0021_chat_mode.sql`, `drizzle/meta/_journal.json`, `src/server/schema.ts`, `src/server/db/specification-store.ts`, `src/server/db.ts`, `src/server/secondary-chat-route.ts`, `src/server/app.ts`, `src/server/core.ts`, `src/shared/api-types.ts` (mode parts), `src/server/chat-substrate.test.ts`, `src/server/app.test.ts`, `secondary-chat-collapsible.tsx`, `secondary-chat-collapsible.test.tsx`, mode parts of `secondary-chat-trigger.tsx`, mode wrapper in `-workspace-transcript-artifacts.tsx`, `memory/CARDS.md`.
   - **Split notes**: `secondary-chat-trigger.tsx` and `-workspace-transcript-artifacts.tsx` were each touched by both C3c-wire and C4. Easiest pragmatic split is to commit them whole under C4 with a sentence in the C3c-wire commit body noting that the mode wiring follows in C4. Alternative: use `git add -p` to stage the create-side first and the mode-side second.
2. After commits land, follow `docs/praxis/graphite-workflow.md` to `gt restack` if needed and `gt submit` the new commits onto the existing FE-716 PR. Do NOT push without user confirmation (`git push` and `gt submit` are user-gated per AGENTS.md).
3. Move on to **C5 — In-thread patch staging on secondary chats**. Use `ln-scope` first if the V3.1 staging UI from PR #138 doesn't drop in cleanly. Then `ln-build`.
4. Once C5 is in flight, delete or overwrite this `HANDOFF.md`.

## Retirement rule

- Delete or overwrite this file once the volatile state above is absorbed into commits + a newer `HANDOFF.md`. Specifically: after step 1 (commits) and step 3 (C5 starts), this handoff is fully reconciled.

## Open questions

- **Commit splitting strategy** for files touched by both C3c-wire and C4 (`secondary-chat-trigger.tsx`, `-workspace-transcript-artifacts.tsx`). Default suggested above is "land whole under C4, mention forward-reference in C3c-wire body". Confirm with user or use `git add -p`.
- **Mode wiring into streaming**: C5 mentions Edit-mode patch staging but the secondary chat doesn't have a streaming endpoint yet. C5 may need to land both (a) the streaming endpoint that calls `getSideChatTools(chat.mode)` and (b) the in-thread staging UI. Check whether C5 needs an `ln-scope` decomposition before building.
- **Agent-run flavor (C7)**: HANDOFF default is "project from `first_turn_role='system'`, don't add a fifth `chat.kind`". Revisit when starting C7.

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root. It contains the full state of FE-716 in-progress work on branch `ka/fe-716-chat-runtime-unified-secondary-chats`.
> The immediate next step is: split the uncommitted C3c-mount + C3c-wire + C4 work into three FE-716 commits matching the existing card-per-commit pattern.
> Start by reading the "Next steps" section of HANDOFF.md, then `git status` to confirm the dirty file list, then create the three commits in order. Do NOT `git push` or `gt submit` without explicit user confirmation.
