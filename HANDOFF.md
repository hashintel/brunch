# Handoff

> Generated by `ln-handoff` at 2026-07-08 12:50 (UTC+2); updated 2026-07-08 13:30 after the FE-1169 planning session. Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Two lanes now open:

1. **Tie off `exchange-ask-refinement` (FE-1164)** — the branch carries everything the frontier owed (walkthrough fixes, witness-audit closures, runtime-mount + candidates probes, doc sync); `gt submit` + PR description still owed. Then the queued lanes: FE-1163 sweep stacks on it; FE-1159 awaits merge → publish; FE-1167 runs after FE-1164 merges.
2. **Scope FE-1169 (`main-editor-chrome`)** — planned and committed 2026-07-08 (grill → plan, commit `f072e309` on `ln/fe-1169-editor-chrome`, stacked on FE-1164). Six threads in `memory/PLAN.md`; next step is `ln-scope` for the thread-1 tracer (`BrunchEditorComponent` via `ctx.ui.setEditorComponent` — render-height assumption, headless guard, border-semantics seam). The `develop`-mode / `engineer`-agent idea was split to Horizon `develop-mode` (D98-L/req-26/D40-L revision via `ln-grill`/`ln-spec` at pickup).

## Session State

- **Last completed skill**: `ln-plan` (2026-07-08 13:26 session) — FE-1169 frontier reframed via `ln-grill` into six threads and committed; Linear issue FE-1169 created + updated to match.
- **Flow position (FE-1169 lane)**: grill → plan → **scope** (next).
- **Flow position (FE-1164 lane)**: unchanged since 12:50 — sync → walkthrough → witness → scope → oracles → build → handoff; tie-off pending.

## In-flight work

> CRITICAL: These artifacts exist only in the prior conversation, not on disk.

### Walkthrough checklist — FE-1164 ask surfaces (items 4–6 + gallery still open)

The 7-item outer-loop checklist for this branch, driven in `.fixtures/workbenches/workspace-alpha-grounding` (`npm run dev -- --workspace .fixtures/workbenches/workspace-alpha-grounding`; reseed with `npm run seed -- --seed workspace-alpha-grounding/base --reset` for a fresh session):

1. Standalone `ask` free-text/single/multi — ✅ user drove an 8-permutation battery; found F18–F20, all fixed (`5ffb3269`) — **free-text and comment variations not re-observed since the fixes**.
2. Declared continuations (digest/review/candidates chain live) — **open** (not yet driven live).
3. Cancel/escape on the ask surface — partially observed; automated by the runtime-mount battery.
4. Capture sweep after answers — **open, newly meaningful**: the sweep-classifier fix (`64aad51a`) is exactly what makes answered material reach capture; a live session proving graph writes land after ask answers is the outer witness.
5. Resume re-render — **open**: quit + relaunch same workbench; persisted ask results must re-render identically.
6. Web sidecar during an open ask — **open**: "pending exchange" is now projected from open ask calls / undischarged declared continuations (D116-L); confirm the sidecar behaves while an ask is open.
7. `npm run dev:components` gallery in both themes — **open for re-check**: the answer-editor preview entry now uses the rich multi-line body (F18 regression guard).

The workbench's `.brunch/` currently holds the user's battery session (pre-fix state); reseed with `--reset` before re-driving.

### Roving-suite flake — evidence detail beyond the PLAN note

- Builder: `npm run verify` timed out twice consecutively in `src/app/__tests__/git-host-promotion-port.test.ts` (full-suite only); the same file passes in isolation.
- This machine, same commit: full suite passed once (224 files / 1519 tests / ~25s). So: load- or machine-sensitive, not deterministic.
- Prior occurrences: FE-1164 review-fix verify (first run), and the pre-drop history that led to the 2026-07-08 action-or-drop call.
- Suspect class named in PLAN: cross-suite resource contention (worker count, temp-git-repo churn, file-lock collision). `ln-diagnose` is owed; the repro loop probably needs repeated full-suite runs under load, not isolation runs.

### Review findings

All findings from this session's audits are addressed; no deferred review debt from this session:

| # | Finding | Status | Implications |
| --- | --- | --- | --- |
| F18 | Free-text ask box corruption (multi-line body as one line) | `addressed` (`5ffb3269`) | Re-observe live (checklist item 1 residue) |
| F19 | Free-text commentPrompt ignored | `addressed` (`5ffb3269`) | Schema widened with `answered.comment` |
| F20 | Optional comment not skippable | `addressed` (`5ffb3269`) | commentPrompt presence now opt-in semantics |
| W1 | Sweep classifier excluded `ask` toolNames (capture regression) | `addressed` (`64aad51a`) | I57-L updated; fixture-vs-real drift class — stay alert for it in FE-1163 |
| W2 | Ask render-honesty missing; `ask-tuples.md` had no writer test | `addressed` (`64aad51a`) | Five-branch golden now writer-owned |
| W3 | No runtime-mount oracle for registered ask | `addressed` (`7ebdcf91`) | InteractiveMode plumbing blind spot recorded in SPEC with triggers |
| W4 | No candidates supersession probe (I57-L named gap) | `addressed` (`7ebdcf91`) | I57-L gap sentence replaced with probe citation |

### Diagnostic evidence

- Empirical sweep probe (tsx one-off): `toolName: 'ask'` toolResult absent from `projectCaptureSweepWindow` tail while `request_review` passed — proved W1 before any fix.
- F18 root cause: `ExchangeAnswerEditorComponent` rendered the whole body as one accent-colored array element; embedded `\n` broke rounded-box borders and differential height accounting (screenshot: second body line rendered outside the box; typing ate lines below).
- Digest supersession proof's green-while-wrong state: fixtures hand-wrapped terminals under retired `toolName: 'request_response'` — the concrete instance of the fixture-vs-real rival class.

## Decisions and assumptions

| Item | Type | Status | Source |
| --- | --- | --- | --- |
| commentPrompt presence gates the optional-comment step (omit = skip; required comments unaffected) | decision | persisted (code + param descriptions + F20 entry) | TESTING_FINDINGS.md Beat 6 |
| Runtime-mount oracle stops at Brunch's `ctx.ui.custom` factory contract | decision | persisted | SPEC §Acknowledged Blind Spots (ask InteractiveMode plumbing row) |
| Sweep admits `ask` + legacy `request_*` toolNames | decision | persisted | sweep-watermark.ts comment + I57-L |
| A33-L retired (validated; residue in D100-L) | decision | persisted | SPEC comment at former row |

Nothing decision-shaped remains volatile.

## Repo state

- **Branch**: `ln/fe-1169-editor-chrome` (checked out), stacked on `ln/fe-1164-ask-terminal` → `ln/fe-1115-tui-refinements-1` (PR #301) → `ln/fe-1159-alpha-release` → `next`. FE-1163 also stacks on FE-1164.
- **Recent commits**: `f072e309` FE-1169 plan reframe (this branch) · on FE-1164: `6f994ee9` flake re-trigger record · `7ebdcf91` witness-gap probes · `61781ed5` doc sync + scope card · `64aad51a` witness-audit closures · `5ffb3269` F18–F20 fixes
- **Dirty files**: none (clean tree; this HANDOFF.md is intentionally untracked-then-committed as volatile state)
- **Test status**: full suite green on this machine post-`6f994ee9` predecessor (224 files / 1519 passed / 2 skipped, ~25s); builder saw the flake under `verify` twice (see above)

## Artifact status

| Artifact | Exists | Current vs conversation |
| --- | --- | --- |
| memory/SPEC.md | yes | current (sync pass + witness reconciliation landed) |
| memory/PLAN.md | yes | current (incl. flake re-trigger note) |
| memory/cards/ | 3 files | `tool-schema-convergence--ledger.md` active (next build); `walkthrough-batch-2--seed-variants.md` active (FE-1167 input); `executor-run-integrity--plan-projection.md` retained pending KA conversation (GC via FE-1167) |
| memory/REFACTOR.md | no | n/a |

## Next steps

1. **Scope FE-1169 thread 1** (`ln-scope` on `ln/fe-1169-editor-chrome`): the `BrunchEditorComponent` persistent-editor tracer — resolves the render-height assumption, fixes `workspace-dialog-headless-guard`, opens the border-semantics seam. Scope-time note from the grill: threads 2 and 3 both touch the exchange formatters and should land adjacently to avoid churning the goldens twice.
2. **Tie off FE-1164**: `gt submit` on `ln/fe-1164-ask-terminal` (read `docs/praxis/graphite-workflow.md` first; PR title convention `FE-1164: One-shot ask terminal and exchange presentation refinement`). PR description gets written now — this is the tie-off. FE-1169 restacks on top afterward.
3. **Finish the walkthrough residue** (items 1-residue, 2, 4–7 above) either pre-submit on FE-1164 or fold into FE-1167's beats — user's call; item 4 is the highest-value one (outer witness of the sweep fix).
4. **Build FE-1163** (`ln-build memory/cards/tool-schema-convergence--ledger.md`) once the stack base is settled.
5. **Schedule `ln-diagnose`** for the roving-suite flake (owed per PLAN; evidence above).

## Open questions

- Does the user want walkthrough items 2/4–6 witnessed on the FE-1164 branch before submit, or explicitly deferred to FE-1167's beats (which already must witness ask surfaces)?
- FE-1159 (#299) merge → publish is human-gated (npm auth); timing is the user's call.

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root. It contains the state of two open lanes: the FE-1164 tie-off (`ln/fe-1164-ask-terminal`, awaiting `gt submit`) and the freshly planned FE-1169 chrome frontier (`ln/fe-1169-editor-chrome`, awaiting `ln-scope` of its thread-1 editor tracer).
> Ask the user which lane to pick up; the FE-1169 frontier definition in `memory/PLAN.md` is the canonical shape for scoping.
