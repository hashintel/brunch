# Handoff

> Generated by `ln-handoff` at 2026-06-30T23:58Z. Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Tie off FE-1089 (`orchestrator-alpha-cutover`) — the descriptive `fs`-only cook lifecycle scaffold — and get PR #274 merged into `next`, with planning docs reconciled into the `orchestrator-cutover` arc (`cook-sandbox` → `cook-agent-runner` → `cook-land`).

## Session State

- **Last completed skill**: `ln-design` (earlier in this work) chose the cook real-execution seam; this session was post-build **delivery work**: doc reconciliation, PR open, Linear comment, and an `origin/next` conflict merge. No new `ln-*` skill ran this session beyond `ln-handoff`.
- **Current skill**: `ln-handoff`.
- **Flow position**: `... → build → [review done] → delivery/tie-off`. FE-1089 build is complete; remaining work is merge + arc kickoff.
- **Handoff trigger**: user requested handoff after PR description finalized and merge conflict resolved.

## In-flight work

> Most durable state is already on disk (committed + pushed). The volatile items below are the few things NOT yet captured in canonical docs.

### Pending canonical reconciliation

- **FE-1089 frontier-def `Linear` field** in `memory/PLAN.md` still shows the issue link but the issue is **not yet set to Done** — Linear FE-1089 is still `In Progress` (FE team has no "In Review" state). Move to Done **only when PR #274 merges**.
- The PLAN/SPEC reconciliation for the arc split **is committed** (commit `f32f2be7`) and survived the merge — no pending doc edits.

### Arc kickoff not yet started (next real work)

The three real-execution frontiers exist as **frontier definitions only** in `memory/PLAN.md`; none have Linear issues or branches yet:

- `cook-sandbox` — `GitWorktreePort` + `TestRunnerPort` (subprocess only, no LLM). **Next up; design chosen, ready to `ln-scope`.** First slice: the git-worktree port. Per AGENTS.md workflow, starting it needs: (1) new Linear issue in FE team / brunch project (do NOT parent under FE-531), (2) `gt`-tracked branch stacked on `ka/fe-1089-orchestrator-alpha-cutover`.
- `cook-agent-runner` — `AgentRunnerPort` (write-capable CODE worker, D90-L–D93-L substrate). After sandbox.
- `cook-land` — `GitLandPort` (run-local then host). Last; only hard-to-reverse seam.

Chosen seam (SPEC D99-L cook-execution-ports refinement, committed): types-only `src/orchestration/cook-execution-ports.ts` defining `CookExecutionPorts = { GitWorktreePort, AgentRunnerPort, TestRunnerPort, GitLandPort }`; real impls in app layer (`src/app/cook-*.ts`), injected by Pi adapters; preserves D52-L/I52-L.

### Review findings

| #   | Finding | Status | Implications |
| --- | ------- | ------ | ------------ |
| 1   | Live executor allowlist (`src/agents/runtime/executor/active-tools.ts`) did not admit the `execute_*` tools after the FE-1091 merge — feature would be registered-but-inactive | `addressed` | Added all `execute_*` tool names to `EXECUTOR_ALLOWED_TOOL_NAMES`. |
| 2   | `next`'s command/mode surface still rejects switching INTO execute mode (`src/.pi/extensions/commands/index.ts`) | `deferred` | Execute mode not user-selectable yet. Out of scope for this merge; revisit when a cutover frontier needs live execute mode. Flagged to user. |
| 3   | FE-1089 execute method skills orphaned in retired `methods/` after FE-1091 quarantined the skill system | `addressed` | Moved `scope-execution-task` + `build-with-tests` to `src/agents/skills/_suspended/methods/`; guidance inlined into `src/agents/prompts/executor.md`. |
| 4   | I52-L invariant row referenced deleted `prompt-resources.test.ts` | `addressed` | Rewrote thinned I52-L to cite surviving `registry.test.ts` + `orchestration/__tests__/*`; noted live routing re-homed to FE-1091 executor path. |

### Diagnostic evidence

- `git rev-list --left-right --count origin/next...HEAD` → `0 34`: branch is 34 ahead, 0 behind `next`. Merge brought it current.
- The whole conflict came from a single merged PR on `next`: **FE-1091 #270** ("Renderer golden coverage and prompt assembly lock, Part 2") — established a "live elicitor/executor" path and quarantined the legacy prompt-resource skill machinery into `_suspended/`.
- Oracle confirmed (with file evidence): `next` no longer loads method-style prompt-resource skills on any live path; `_suspended` is excluded from verify (`tsconfig.json`, `vite.config.ts`); the live executor prompt is body + control only.
- `npm install` was required — FE-1091 added `remark-cli` / `remark-validate-links` (new `check:markdown-links` gate) not present in this worktree's `node_modules`.

## Decisions and assumptions

| Item | Type | Status | Source |
| ---- | ---- | ------ | ------ |
| Real cook execution enters via injected `CookExecutionPorts` bag (not deep env object, not effect-program rewrite) | `decision` | `persisted` | SPEC.md §D99-L cook-execution-ports refinement |
| Host land blocked until real-execution substrate exists; faking host git mutation rejected under I52-L | `decision` | `persisted` | SPEC.md §D99-L land-substrate finding |
| FE-1087 (`orchestrator-tool-port`) superseded by FE-1089 | `decision` | `persisted` | PLAN.md frontier def |
| Execute mode not user-selectable yet (mode picker rejects it on `next`) | `assumption` | `volatile` | Oracle review; commands/index.ts |

## Repo state

- **Worktree**: `/Users/kostandin/Projects/hashdev/brunch-wt/falcon`
- **Branch**: `ka/fe-1089-orchestrator-alpha-cutover` (34 ahead of `origin/next`, 0 behind)
- **Recent commits**:
  - `dd824778` Merge remote-tracking branch 'origin/next' (conflict resolution)
  - `f32f2be7` Split FE-1089 cutover into orchestrator-cutover arc frontiers
  - `f8cf3674` Reorder FE-1089: defer host land to cook-real-execution
  - `055ff06e` Add execute cook promotion prepare
- **Dirty files**: none (clean)
- **Test status**: `npm run verify` green — 163 test files, 1102 passed, 1 skipped, 0 errors (3 pre-existing `unicorn(no-thenable)` warnings in `src/graph/schema/nodes.ts` and two graph test files).
- **PR**: [#274](https://github.com/hashintel/brunch/pull/274) → base `next`, state OPEN, **mergeable CLEAN**. Description rewritten (Features / Structure). NOT a Graphite-tracked branch (pushed via plain git; `gh pr create --base next`).
- **Linear**: FE-1089 still `In Progress`; comment posted linking PR #274.

## Artifact status

| Artifact           | Exists | Current vs conversation |
| ------------------ | ------ | ----------------------- |
| memory/SPEC.md     | yes    | current (committed)     |
| memory/PLAN.md     | yes    | current (committed)     |
| memory/CARDS.md    | no     | n/a                     |
| memory/REFACTOR.md | no     | n/a                     |

## Next steps

1. **Wait for / land PR #274** into `next` (it is CLEAN + MERGEABLE). After merge: set Linear FE-1089 → Done.
2. **Start `cook-sandbox`**: run `ln-scope` on it. Per AGENTS.md, first create an FE Linear issue (brunch project, no FE-531 parent). **Resolve branch tracking first** (see Open questions): FE-1089 is currently NOT Graphite-tracked, so a naive `gt create` stacked on it will have ambiguous parentage or a broken stack. Either `gt track --parent <parent>` FE-1089 into the stack before stacking `cook-sandbox` on it, or continue the plain-git + `gh pr --base ka/fe-1089-...` (or `--base next` after #274 merges) pattern used for FE-1089. First slice = the `GitWorktreePort` (real `git worktree add` replacing `cook-worktree.ts`'s `mkdir`).
3. Optional follow-up (deferred review finding #2): decide whether to make execute mode user-selectable (`src/.pi/extensions/commands/index.ts`) — likely belongs to a cutover frontier, not standalone.

## Retirement rule

- Delete or overwrite this file once PR #274 is merged and `cook-sandbox` scoping begins (its volatile state will then live in a scope card / new branch).

## Open questions

- Should `cook-sandbox` (and the arc) stack in Graphite, or continue the plain-git + `gh pr --base next` pattern used for FE-1089? The FE-1089 branch is currently untracked in Graphite.
- When should execute mode become user-selectable — as part of `cook-sandbox`, or a dedicated small frontier?

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in `/Users/kostandin/Projects/hashdev/brunch-wt/falcon`. It contains the full state of in-progress FE-1089 work.
> The immediate next step is: check whether PR #274 merged; if so set Linear FE-1089 → Done, then start `cook-sandbox` with `ln-scope`.
> Start by reading the `cook-sandbox` frontier definition in `memory/PLAN.md` and the D99-L cook-execution-ports refinement in `memory/SPEC.md`.
