# Handoff

> Generated by `ln-handoff` at 2026-07-07 16:18 (UTC+2). Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

Close out the FE-1115 TUI-refinement lane's open ends (theme values Card 4, three outer manual checks), then pivot to the **next declared direction: refinement of the structured-exchange tools** (user, session end).

## Session State

- **Last completed skill**: `ln-build` — Card 5 (scrollback-safe working indicator) built, verified (1517 tests), committed `12cb0b3c`. Earlier in the same session: Cards 1–3 built and committed; a mid-session `ln-sync` (commit `4adcf266`) folded lane state into PLAN and retired the 2026-07-06 handoff.
- **Current skill**: `ln-handoff`.
- **Flow position**: consult → scope → build (×5 cards, interleaved with user manual testing) → sync → build → **handoff**. No `ln-review` ran on the lane's ~10 commits.
- **Handoff trigger**: user-requested; next session pivots to structured-exchange tool refinement.

## Branch / repo state

- **Branch**: `ln/fe-1115-tui-refinements-1`, stacked on `ln/fe-1159-alpha-release` → `ka/fe-1107-…` → `ka/fe-1154-…` → `ka/fe-1141-…` → `next`. **Not submitted** — no `gt submit`, no PR yet.
- **Worktree**: clean. The user committed the iterated theme JSONs themselves as `0f1e21cf` ("re-order and normalize the theme files").
- **Session commits** (all FE-1115 unless noted): `6fb99f2f` theme toggle · `1d7dd87b` OSC 11 page bg · `b4583a72` harness matchesKey · `edcf6726` production matchesKey sweep · `f587e046` hot reload + testbed · `7b6e8bc3` OSC 10 fg · `7dc38e4b` SGR fallback + pageFg seam · `4adcf266` sync · `0f1e21cf` theme JSONs (user) · `12cb0b3c` working indicator.
- **Tests**: full `npm run verify` green at `12cb0b3c` (1517 passed).

## In-flight work

### 1. Next direction: structured-exchange tool refinement (nothing scoped yet)

User declared this the next work area; no grill/scope has happened. Known entry points already in canonical docs:

- PLAN Horizon: `review-commentary-widening` (per-item review comments; payload ripples into review schema) and `blank-carrier-sweep` (extend `zNonBlankMarkdown` to candidate rubric fields, `zPresentOption.content`, `zAnsweredOptionEcho.content`).
- Unscheduled follow-ups noted at FE-1136 review (PLAN Recently Completed): hoist the 5× duplicated `normalizeOptionalText` in `src/exchanges/projections/`; a derived per-kind terminal mapping to replace hand-taught `exchange-projection.ts` rows.
- Rendering seam: all `present_*`/`request_*` transcript renders are flat Markdown pass-through per D104-L (`renderMarkdownResult` in `src/.pi/extensions/exchanges/shared/markdown.ts`); the preview harness has entries for every kind — refinement can iterate there (plus ctrl+t/hot-reload from this lane).
- Route: `ln-consult` or `ln-grill` first — "refinement" is not yet a defined scope.

### 2. component-dx lane — open ends (scope file: `memory/cards/component-dx--preview-theme-toggle.md`)

- **Card 4 (theme values) — [in progress] and its note is stale**: the card says "JSONs deliberately uncommitted" but the user committed them (`0f1e21cf`). Update the card when next touched. Open value items recorded there: light `mediumGray` `#8B857E` (3.5:1) and `syntaxComment` `#8CAF59` (2.4:1) below the 4.5:1 floor vs `#f9f9f9`. User said "I'll work more on the theme later" — Card 4 stays open.
- **Outer manual checks owed** (named in the card): Card 5 — live session: `● Working…` stays static, scrollback holds during a long thinking stretch, OSC 9;4 progress visible in Ghostty. Card 1 residue — deep-link path check (`npm run dev:components -- brunch-editor` with ctrl+t) was never explicitly confirmed.
- Cards 1–3, 5 are `done` in the scope file with divergences noted.

### 3. Flaky test observed (unpersisted evidence — relevant to the next direction!)

`src/probes/__tests__/structured-exchange-ordering-proof.test.ts` failed once under full-suite parallel load (gate at ~14:53), then passed twice in isolation and in every subsequent full gate. Timing-sensitive probe, pre-existing, unrelated to this session's changes. **Since the next session works structured-exchange, treat a failure there as possibly-flake first: rerun in isolation before diagnosing.** Candidate `ln-diagnose` target if it recurs.

### 4. TUI concerns backlog (user: "we'll need to do most of these")

Acknowledged but unscoped, in the user's implied order after exchanges: `main-editor-chrome` (Horizon frontier, de-risked by FE-1138), footer lane preview (`ui.setFooter`, no dev:components entry), `workspace-dialog-headless-guard` (Horizon), real-terminal wheel smoke + mouse-mode ownership.

### 5. Review debt

No `ln-review` ran over the lane's commits. Notable self-known rough edges: `theme-testbed.ts` installs a bound facade at pi's global theme symbol (documented, but it's the harness's deepest pi-internals coupling); `createThemePaintingTerminal`'s escape-rewrite assumes whole-frame writes (marked `ceiling:`).

## Decisions and assumptions (this session, all persisted)

| Item | Where |
| --- | --- |
| Live sessions keep pi's harmonize-don't-override stance; harness paint is reference-page simulation only | scope file Card 4 + `src/dev/TOPOLOGY.md` |
| `text: ""` (pi canonical terminal-default); concrete ink → `ink` var for themed-surface slots only | theme JSONs + Card 4 |
| Neutral reference environment `#0b0b0b`/`#f9f9f9`; fg defaults in code (`REFERENCE_PAGE_FG`) because pi's schema strips unknown `export.pageFg` | `theme.ts` + Card 5 note |
| `matchesKey`-only key handling convention (kitty protocol) | `src/.pi/components/TOPOLOGY.md` |
| Static working indicator + OSC 9;4 liveness (option 1+3) | chrome extension + `BRUNCH_SETTINGS_POLICY` + Card 5 |

## Evidence worth keeping (verified against pi 0.80.3 dist + source clone)

- `fgAnsi("") → \x1b[39m` (theme token `""` = terminal default) — theme.ts:261 in pi source.
- `Loader.restartAnimation` starts no interval when `frames.length <= 1`; `ctx.ui.setWorkingIndicator` persists options; RPC mode stubs it.
- Kitty protocol: ProcessTerminal negotiates flags 1+2+4; presses arrive as CSI-u (`\x1b[1;1:1B` etc.); `matchesKey` handles both encodings; input listeners see key releases (TUI filters them only for focused components).
- OSC 10/11 honored by Ghostty, ignored by Zed's terminal; OSC 111/110 reset; `\x1b[K` erases with active bg (BCE) — basis of the SGR fallback.

## Artifact status

| Artifact | State |
| --- | --- |
| memory/SPEC.md | current; **overweight (~369K chars)** — compression pass flagged in sync report, unscheduled |
| memory/PLAN.md | current as of `4adcf266` + this lane's Card 5 (component-dx pointer mentions Cards 1–3; Card 5 done since — minor staleness) |
| memory/cards/component-dx--preview-theme-toggle.md | live (Card 4 open; Card 4 "uncommitted JSONs" note stale) |
| memory/cards/walkthrough-batch-2--seed-variants.md | live (Card 3 remains; untouched this session) |
| memory/cards/executor-run-integrity--plan-projection.md | retained-unconfirmed pending KA conversation |
| docs/DEMO_STACK_OVERVIEW_2026-07-06.md | live until KA session; then delete |

## Next steps

1. **Start the structured-exchange refinement**: run `ln-consult` (or `ln-grill` if the user has specific refinements in mind) to define scope; check the Horizon items and FE-1136 follow-ups listed above as candidate material. Decide whether it rides FE-1115's branch (presentation-only refinement) or needs its own frontier/branch per AGENTS.md (likely the latter if it touches schemas/projections).
2. When convenient: the three outer manual checks (Card 5 live-session, Card 1 deep-link); Card 4 staleness fix.
3. Eventually: `gt submit` the lane branch; `ln-review` over the ~10 commits before tie-off.

## Retirement rule

Delete or overwrite this file once the structured-exchange work is scoped (its own card/frontier) and the Card 4/5 residue is either done or re-recorded in the scope file.

## Resume prompt

> Read `HANDOFF.md` at the workspace root. We are pivoting to refinement of the structured-exchange tools; the FE-1115 TUI lane is committed but unsubmitted with small residue. Start with `ln-consult` to classify and scope the exchange-refinement work — candidate material is listed under "In-flight work §1". Note the flaky `structured-exchange-ordering-proof.test.ts` warning before trusting any test failure there.
