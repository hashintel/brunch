# Handoff

> Generated by `ln-handoff` at 2026-06-02. Read this file to resume work.
> Volatile transfer state only. Overwrite/delete once reconciled into canonical docs.

## Goal

Stream an executing `brunch cook` run live into Petrinaut's read-only "actual/live" tab for the Bristol demo — the reshaped **FE-764** (`petri-sync-server`).

## Session State

- **Last completed skill**: `ln-plan` — rewrote the FE-764 frontier in `memory/PLAN.md` (ephemeral SSE design) and fixed adjacent drift (FE-761/762/763 → done, FE-784 → done/set-aside).
- **Current skill**: `ln-handoff`.
- **Flow position**: `grill → spec → plan → [design] → [oracles] → **scope** → [spike] → build → review → [refactor] → [sync]` — next intended step was `ln-scope`.
- **Handoff trigger**: user requested checkpoint; **mid-handoff a data-loss + branch-mismatch was discovered and repaired** (see In-flight work).

## ⚠️ Branch + data-loss event (read first)

- The session began on `ka/fe-763-petri-petri-event-stream` but the working tree was checked out across the Graphite stack during the session (reflog: `fe-763 → fe-784 → fe-761 → fe-762 → fe-763 → fe-784`). **It now sits on `ka/fe-784-petrinaut-colour-fold`.**
- Three artifacts created this session were untracked and **vanished** during that branch-hopping. They have been **RESTORED** during this handoff (content was preserved in-conversation; the reducer survived in `/tmp`):
  - `src/orchestrator/src/petrinaut-stream-contract.ts` — restored, compiles (`npm run check` 0 errors).
  - `docs/petrinaut-stream-contract.md` — restored.
  - `.brunch/cook/runs/904d205d-.../petrinaut-export.json` — regenerated via `/tmp/reduce-export.mjs` (oracles pass).
- **UNRESOLVED — your decision:** FE-764 work (the restored files + the `memory/PLAN.md` edit) is currently sitting **untracked/modified on the `fe-784` working tree**, which is a *different* (done) frontier. FE-764 should likely get its **own Graphite branch** stacked appropriately, and these changes moved/committed there before the next checkout loses them again. Until then, these files are at risk on any branch switch.

## In-flight work

### The contract (RESTORED to disk)

`BrunchExecutionExport` = one net definition + a sequence of markings. `initialState` and every firing's `input`/`output` share one `Marking` type.

- `definition` = SDCPN topology **minus `scenarios`** (`Omit<SdcpnFile,'scenarios'>`). Lambdas/kernels are inert placeholders (Petrinaut replays, doesn't simulate).
- `Marking = Record<PlaceId, number | TokenColour[]>` — **≡ Petrinaut's runtime `InitialMarking`** (`@hashintel/petrinaut-core` `simulation/api.ts`). Demo uses only the `number` (count) arm. Coloured arm is `Record<string, number>` (element-name-keyed) — NOT the earlier `{id,value}` sketch.
- `transitionFirings: { transitionId, input: Marking, output: Marking, ts }[]` — per-place count **deltas**.
- Petrinaut compatibility **verified** against `parse-sdcpn-file.ts`, `file-format/types.ts`, `simulation/api.ts`, `frames/*`, `use-firing-delta.ts` (read from `hashintel/hash` repo via `gh api`).

### Locked design decisions (from 2026-06-01 meeting + 2026-06-02 scoping)

1. **Ephemeral cook-process-hosted SSE server** — boots on a free port, dies with the run, **nothing persisted**.
2. **One SSE stream, replay-on-connect**: `definition` (once) → `initial_state` (once) → all firings-so-far → live `transition_firing` → terminal.
3. **No discovery/list endpoint** — Brunch initiates the session.
4. **Two trigger surfaces**: (a) cook **CLI flag** emits/auto-opens the URL; (b) brunch **web-UI button** must also be available (open: how it discovers the ephemeral endpoint).
5. **Reuse cook `runId`** as the session id.
6. **NO colours for the demo** — unfolded per-slice net + counts (inverse of FE-784). Export path needs an **identity/no-op fold** to bypass `createNetFolding`.
7. Read-only, one-way, localhost.

### Key architecture finding (informs all infra work)

- `cook` is a **detached one-shot CLI process** (`src/server/cli.ts` → orchestrator `engine.ts`); it writes `<runDir>/{net.json, net.sdcpn.json, petrinaut-events.jsonl}` and exits. The event stream's `onEvent` hook (`petrinaut-events.ts`) is the in-process seam to feed SSE.
- Brunch ALSO has a persistent Express(`:3000`)+Vite(`:5173`)+React app — but it's the **specs/interview app**, with **no web-UI ↔ cook link today**.
- Petrinaut website (`apps/petrinaut-website` in `hashintel/hash`) is localStorage-based with **no URL-param / "actual" mode yet** — Chris is building exactly that. So the URL param scheme is ours to define jointly.

### Verification approach (frame-replay oracle — already passing)

The reducer `/tmp/reduce-export.mjs` proves the contract end-to-end on real run `904d205d`: reduce SDCPN-minus-scenario + count-reduce markings, then **reconstruct all 75 frames** from `initialState` + deltas → **0 negative-marking violations**, final marking sane (pools 5/5, `epic:scaffolding:done`, verify mid-flight). This is the unit oracle for the export reducer slice.

### Review findings

None — no `ln-review` ran this session.

### Diagnostic evidence

- `git reflog -8` → the branch-hopping that lost the untracked files (see above).
- `ls` + `git status --porcelain` → confirmed the 3 artifacts were absent before restore; only `memory/PLAN.md` was tracked-modified.
- Petrinaut `simulation/api.ts`: `InitialPlaceMarking = number | Record<string,number>[]` → proved the no-colour `number` arm is exactly compatible and the `{id,value}` sketch was wrong.

## Decisions and assumptions

| Item | Type | Status | Source |
| --- | --- | --- | --- |
| Ephemeral cook-hosted SSE; replay-on-connect; no discovery; reuse runId; no-colour; dual trigger | decision | persisted (PLAN.md FE-764) | conversation 2026-06-01/02 |
| `Marking` ≡ Petrinaut `InitialMarking`; coloured arm = `Record<string,number>` | decision | persisted (contract .ts) | verified vs @hashintel/petrinaut-core |
| FE-764 work belongs on its own branch (not fe-784) | assumption | **volatile — needs user decision** | conversation |

## Repo state

- **Branch**: `ka/fe-784-petrinaut-colour-fold` (FE-764 work is here but probably shouldn't be).
- **Recent commits**: `1c21816c Fix folded event stream test setup`, `12552657 Preserve dep-signal endpoints in Petrinaut fold`, `d7552353 FE-784: Align fold identifiers to color`.
- **Dirty/untracked**: `M memory/PLAN.md`; restored-untracked `src/orchestrator/src/petrinaut-stream-contract.ts`, `docs/petrinaut-stream-contract.md`; regenerated `.brunch/cook/runs/904d205d-.../petrinaut-export.json` (gitignored).
- **Test status**: `npm run check` → 0 errors, 7 pre-existing warnings (none in new files). Full `npm run verify` not run this session.

## Artifact status

| Artifact | Exists | Current vs conversation |
| --- | --- | --- |
| memory/SPEC.md | yes | current (no edits needed this session; §Lexicon already carries folded-net/color-fold) |
| memory/PLAN.md | yes | current — FE-764 reshaped, drift fixed |
| memory/CARDS.md | no | n/a (no scope-card queue yet) |
| src/orchestrator/src/petrinaut-stream-contract.ts | yes (restored) | current |
| docs/petrinaut-stream-contract.md | yes (restored) | current |

## Next steps

1. **Decide FE-764's branch** and move/commit the restored files + PLAN.md edit there (per CLAUDE.md workflow: `/cli-graphite`, read `docs/praxis/graphite-workflow.md`). FE-764 Linear issue exists under FE-760. **Do this before any checkout** or the untracked files vanish again.
2. `ln-scope` the first slice: **the export reducer** — `BrunchExecutionExport` from a finished/in-progress run, with the **identity/no-op fold** (bypass `createNetFolding`) + the **frame-replay oracle**. Promote `/tmp/reduce-export.mjs` into `src/orchestrator/src/` + a real test.
3. Then subsequent slices (same frontier/branch, queue in `memory/CARDS.md`): ephemeral SSE server in the cook process; CLI flag + URL emission; web-UI button + endpoint discovery.
4. Send Chris the restored `docs/petrinaut-stream-contract.md` / `petrinaut-stream-contract.ts` (the "today" deliverable). SSE API target ~2026-06-03.

## Retirement rule

Delete/overwrite once the branch decision is made, FE-764 work is committed to its branch, and the next-slice scope card exists.

## Open questions

- URL param scheme + `mode` value (joint with Chris; no Petrinaut scheme exists yet).
- How the web-UI button discovers the ephemeral cook endpoint (e.g. cook advertises `{sessionId,url,port}` to a known location the SPA reads).
- Emit a `net_completed` terminal? (today only `net_halted`/`net_deadlocked` — a clean finish has no terminal event.)
- `meta.generatorVersion` semantics — export-schema version vs brunch app version.

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the brunch workspace root. It contains the full state of the FE-764 Brunch⇄Petrinaut live-streaming work.
> FIRST: the working tree is on `ka/fe-784` but FE-764 work (restored contract files + a PLAN.md edit) is untracked there and will vanish on any branch switch — decide FE-764's Graphite branch and move/commit the changes there before doing anything else.
> Then the next step is `ln-scope` for the export-reducer slice (identity/no-op fold + frame-replay oracle; promote `/tmp/reduce-export.mjs` into src + a test).
