# Run D — Populated codebase, new bare spec: code exploration ingest

setup:

```sh
WORKSPACE=.fixtures/workbenches/manual-code-ingest
rm -rf "$WORKSPACE"
mkdir -p "$WORKSPACE/src"
cat > "$WORKSPACE/src/todo.ts" <<'EOF'
export function groupTodos(items: { done: boolean; title: string }[]) {
  return {
    open: items.filter((item) => !item.done),
    done: items.filter((item) => item.done),
  };
}
EOF

npm run dev -- --workspace "$WORKSPACE" --open-web --dev-tools
```


## new spec in populated cwd



## asks “product vs feature?” / “brownfield?” before assuming



## ingest/explore code rather than docs



## whether code exploration is seeded, requested via tool, or prompted awkwardly



## capture behavior from source-derived facts
