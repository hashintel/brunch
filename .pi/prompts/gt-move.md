---
description: Move the current graphite branch to a new parent
argument-hint: <parent-branch>
---
Run `gt move -o $1` and then follow the instructions to resolve the rebase conflicts, paying special attention to semantically resolving churn in canonical docs
