# Handoff Template

```md
# Handoff

> Generated by `ln-handoff` at [timestamp]. Read this file to resume work.
> This file is volatile transfer state only. After its contents are reconciled into canonical docs or superseded by a newer handoff, overwrite or delete it.

## Goal

[The overarching intent — what we're trying to achieve, in one sentence]

## Session State

- **Last completed skill**: `ln-<skill>` — [what it produced]
- **Current skill**: `ln-handoff` (or other if handoff is mid-skill)
- **Flow position**: `grill → spec → plan → [design] → [oracles] → scope → [spike] → build → review → [refactor] → [sync]`
- **Handoff trigger**: [why the handoff is happening]

## In-flight work

> CRITICAL: These artifacts exist only in the prior conversation, not on disk.
> Reproduce them here with full fidelity.

[Full or light scope cards, plan drafts, design alternatives, oracle designs (including verification state), spike verdicts,
 refactor plans, grill insights, decisions — in their native format, not summarized]

### Review findings

> ALL findings from ln-review, not just the one being acted on.

| #   | Finding | Status                                   | Implications |
| --- | ------- | ---------------------------------------- | ------------ |
| 1   | ...     | `addressed` / `in-progress` / `deferred` | ...          |
| 2   | ...     | ...                                      | ...          |

### Diagnostic evidence

> Concrete proof points that informed diagnoses or shifted direction.
> Without these, a new thread inherits conclusions but not reasoning.

- [evidence]: [what it proved or ruled out]

## Decisions and assumptions

| Item | Type                      | Status                   | Source                                           |
| ---- | ------------------------- | ------------------------ | ------------------------------------------------ |
| ...  | `decision` / `assumption` | `persisted` / `volatile` | SPEC.md §Decisions / §Assumptions / conversation |

## Repo state

- **Branch**: ...
- **Recent commits**: ...
- **Dirty files**: ...
- **Test status**: ...

## Artifact status

| Artifact           | Exists | Current vs conversation   |
| ------------------ | ------ | ------------------------- |
| memory/SPEC.md     | yes/no | current / stale / missing |
| memory/PLAN.md     | yes/no | current / stale / missing |
| memory/cards/      | list   | files: active / stale / n/a per file |
| memory/REFACTOR.md | yes/no | current / stale / n/a     |

## Next steps

1. [First action — must be actionable without clarification. Name the ln-* skill.]
2. ...
3. ...

## Retirement rule

- Delete or overwrite this file once the volatile state above is absorbed into `memory/SPEC.md`, `memory/PLAN.md`, code, or a newer `HANDOFF.md`.

## Open questions

- [Unresolved uncertainties that need human input]

## Resume prompt

Paste this into a new session:

> Read `HANDOFF.md` in the workspace root for this work area. It contains the full state of in-progress work.
> The immediate next step is: [first action from Next steps].
> Start by [specific instruction — e.g., "reviewing the scope card in the In-flight section and running ln-build"].
```
