export const REQUEST_REVIEW_TOOL = 'request_review' as const;

// Stubbed intentionally: review response semantics are named now, but not
// registered until review-set proposal/acceptance flow lands.
export const requestReviewTool = undefined;
