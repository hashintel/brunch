export const PRESENT_REVIEW_SET_TOOL = 'present_review_set' as const;

// Stubbed intentionally: review-set presentation semantics are named now, but
// not registered until review-set proposal/acceptance flow lands.
export const presentReviewSetTool = undefined;
