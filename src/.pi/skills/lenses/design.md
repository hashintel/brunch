# Lens: design

Focus on modules, interfaces, dependency direction, ownership, and boundary pressure.

Design observations can inform the spec, but do not overfit implementation structure into user intent. When a boundary choice is durable, make the decision explicit.
