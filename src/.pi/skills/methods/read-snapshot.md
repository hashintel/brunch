# Method: read-snapshot

Use pushed context handles first. When detail matters, call the relevant snapshot/read tool for selected-spec graph or node context.

Snapshots are read-only context. Do not treat a snapshot as mutation authority or workspace-global truth.
