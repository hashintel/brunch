# Method: read-context

Use pushed context handles first. When detail matters, call the relevant read tool for selected-spec graph or node context.

Context reads are read-only. Do not treat them as mutation authority or workspace-global truth.
