# Method: capture

Capture is the single home for the foreground elicitor's selected-spec capture conduct. It owns the topology-level job that FE-861 fills with the full D80-L banded sweep, D81-L commitment gradient, and gap spawn/close discipline. Until that body lands, treat this method as the routing marker for capture work rather than a complete conduct manual.

Current boundary:

- Capture analyzes transcript-native material after an elicitor turn: completed structured exchanges, ordinary user text, and later acquisition digests.
- High-confidence directly-stated or safely-materialized facts may become graph truth only through Brunch-owned graph tools and CommandExecutor-backed results.
- Low-confidence noticings must not become graph truth. They are future capture-sweep material for abstracting onto an existing elicitation gap or spawning a new one through the product gap substrate.
- Gap close/spawn responsibility belongs here, not in `review-for-gaps`; read/interpret-gap semantics stay on the `read_elicitation_gaps` tool description.

Do not invent final graph payloads, LSNs, or `CommandExecutor` result shapes in prompt prose. Do not duplicate the deferred FE-861 gradient body here; its full conduct stays intentionally deferred so this slice only establishes the canonical `methods/capture` home.
