export const PRESENT_CANDIDATES_TOOL = "present_candidates" as const

// Stubbed intentionally: candidate presentation semantics are named now, but
// not registered until candidate artefact rendering has a product owner.
export const presentCandidatesTool = undefined
