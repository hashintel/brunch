export const REQUEST_CHOICES_TOOL = "request_choices" as const

// Stubbed intentionally: multi-choice response semantics are named now, but the
// implementation waits until the present/request ordering proof is complete.
export const requestChoicesTool = undefined
