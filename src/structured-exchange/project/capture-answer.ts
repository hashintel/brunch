/**
 * Canonical projection for `capture_answer` analysis content.
 *
 * Input:
 * - capture-answer details once the tool lands
 *
 * Output:
 * - normalized captured semantic summary
 *
 * Future users:
 * - structured-exchange/format/capture-answer.ts
 */

export {};
