/**
 * Canonical projection for `capture_choices` analysis content.
 *
 * Input:
 * - capture-choices details once the tool lands
 *
 * Output:
 * - normalized captured semantic summary
 *
 * Future users:
 * - structured-exchange/format/capture-choices.ts
 */

export {};
