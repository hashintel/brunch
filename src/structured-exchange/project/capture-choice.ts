/**
 * Canonical projection for `capture_choice` analysis content.
 *
 * Input:
 * - capture-choice details once the tool lands
 *
 * Output:
 * - normalized captured semantic summary
 *
 * Future users:
 * - structured-exchange/format/capture-choice.ts
 */

export {};
