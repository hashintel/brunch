/**
 * Formats projected `present_review_set` data into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/present-review-set.ts
 *
 * Output:
 * - durable review-set markdown for toolResult.content
 */

export {};
