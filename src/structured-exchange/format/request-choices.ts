/**
 * Formats projected `request_choices` response data into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/request-choices.ts
 *
 * Output:
 * - durable response markdown for toolResult.content
 */

export {};
