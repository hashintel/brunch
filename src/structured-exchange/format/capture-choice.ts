/**
 * Formats projected `capture_choice` analysis into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/capture-choice.ts
 *
 * Output:
 * - capture-side markdown for toolResult.content
 */

export {};
