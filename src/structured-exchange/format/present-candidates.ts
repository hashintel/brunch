/**
 * Formats projected `present_candidates` data into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/present-candidates.ts
 *
 * Output:
 * - durable candidate-comparison markdown for toolResult.content
 */

export {};
