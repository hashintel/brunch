/**
 * Formats projected `request_answer` response data into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/request-answer.ts
 *
 * Output:
 * - durable response markdown for toolResult.content
 */

export {};
