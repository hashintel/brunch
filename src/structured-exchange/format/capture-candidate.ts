/**
 * Formats projected `capture_candidate` analysis into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/capture-candidate.ts
 *
 * Output:
 * - capture-side markdown for toolResult.content
 */

export {};
