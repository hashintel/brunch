/**
 * Formats projected `request_review` response data into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/request-review.ts
 *
 * Output:
 * - durable response markdown for toolResult.content
 */

export {};
