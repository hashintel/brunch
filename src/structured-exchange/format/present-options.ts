/**
 * Formats projected `present_options` data into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/present-options.ts
 *
 * Output:
 * - durable prompt-side markdown for toolResult.content
 */

export {};
