/**
 * Formats projected `capture_answer` analysis into durable markdown.
 *
 * Input:
 * - projected output from structured-exchange/project/capture-answer.ts
 *
 * Output:
 * - capture-side markdown for toolResult.content
 */

export {};
