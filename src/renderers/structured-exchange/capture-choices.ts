/**
 * Formats projected `capture_choices` analysis into durable markdown.
 *
 * Input:
 * - projected output from projections/structured-exchange/capture-choices.ts
 *
 * Output:
 * - capture-side markdown for toolResult.content
 */

export {};
