/**
 * Formats projected `capture_review` analysis into durable markdown.
 *
 * Input:
 * - projected output from projections/structured-exchange/capture-review.ts
 *
 * Output:
 * - capture-side markdown for toolResult.content
 */

export {};
