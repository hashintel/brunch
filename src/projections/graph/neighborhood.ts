/**
 * Deprecated graph-neighborhood projection seam.
 *
 * Node-local graph facts now arrive as `NodeNeighborhood` from graph/queries.ts,
 * and model-facing flattening lives beside the graph renderer. Keep this module
 * as a temporary empty topology marker until the projections ledger is reconciled.
 */

export {};
