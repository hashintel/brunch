/**
 * Canonical projection for `capture_review` analysis content.
 *
 * Input:
 * - capture-review details once the tool lands
 *
 * Output:
 * - normalized captured semantic summary
 *
 * Future users:
 * - renderers/structured-exchange/capture-review.ts
 */

export {};
