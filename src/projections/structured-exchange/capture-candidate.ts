/**
 * Canonical projection for `capture_candidate` analysis content.
 *
 * Input:
 * - capture-candidate details once the tool lands
 *
 * Output:
 * - normalized captured semantic summary
 *
 * Future users:
 * - renderers/structured-exchange/capture-candidate.ts
 */

export {};
