anchor node
- REQ1: Stage 2 must compute three configuration spaces with the semantics defined in T20: M_current (satisfies constraints and current baseline, e…
body: Stage 2 must compute three configuration spaces with the semantics defined in T20: M_current (satisfies constraints and current baseline, excluding alternatives requiring locked-baseline revision), M_preview (includes revision-requiring alternatives tagged as preview-only), and M_revision(r) (after an authorized revision set r is applied).

downstream nodes (2) — reconcile these if anchor changes
- required by D11: Adopt the two-stage split with a hard schema boundary forbidding solver outputs from Stage 1. {hard}
- required by T4: Three configuration spaces are defined: M_current (satisfies constraints and cu… {hard}