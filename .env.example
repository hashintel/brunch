ANTHROPIC_API_KEY=

# FE-764: base URL of the Petrinaut SPA used by `brunch cook --petrinaut-stream`
# to compose the live-stream launcher URL. CLI flag `--petrinaut-base-url=<url>`
# overrides this. No baked-in default — a wrong default silently opens the wrong tab.
PETRINAUT_BASE_URL=
