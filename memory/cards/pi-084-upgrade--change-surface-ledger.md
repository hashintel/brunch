# Pi 0.84.x change-surface sweep

Frontier: pi-084-upgrade
Status:   active
Mode:     sweep
Created:  2026-08-11

## Orientation

- The containing seam is brunch's entire consumption surface over `@earendil-works/pi-coding-agent`, `pi-ai`, and `pi-tui` — 210 importing files and ~110 distinct imported symbols, pinned at `0.83.0` on `next`; the execution-start target is the current common package-family release, `0.84.2`.
- FE-1352 `pi-084-upgrade` is the containing coverage frontier. It stacks on completed FE-1348 and uses that frontier's closed inventory as its regression oracle; the evidence gate is satisfied.
- No `HANDOFF.md` exists. `main`'s PR #422 is a separate trunk's bump and is not this frontier's work; its migration target (`src/orchestrator/src/pi-actions.ts`) does not exist on `next`.
- Main risk: **the changelog is not the inventory.** Scoping found a hard breaking change absent from the published Breaking Changes section (row C1), so rows are enumerated from brunch's import graph — which is closed and derivable — not from changelog entries.

Posture: proving (inherited from `pi-084-upgrade`).

Cross-cutting obligations:

- I64-L single-writer authority and I65-L target-addressed semantic convergence must still hold on the moved baseline; the four `session-runtime-contract-*.slow.test.ts` witnesses are their oracle.
- D19-L's stream↔transcript differential (assembled `message_update` deltas == flushed JSONL) is the linchpin the event rows must preserve — `src/rpc/TOPOLOGY.md:400`.
- Canonical truth stays SQLite graph state plus active-branch Pi JSONL; no row may introduce a second store.
- The `PiClient`/`RemoteSession` verdict (row G1) is owed to the `shared-session-host-convergence` arc before `shared-session-host-cutover` deletes anything.
- Per `docs/praxis/pi-types.md`, a row does not close while its only guard is an untyped structural cast.

## Sweep preflight

### Boundary

**Included:** the three pinned packages plus lockfile; every brunch sub-seam that consumes a Pi symbol, enumerated from the import graph; typed-seam hardening wherever a load-bearing Pi seam is read through a structural cast; a recorded adopt/promote/decline verdict for each new-API area; re-run of FE-1321's four slow witnesses and the required FE-1348 subset; the `brace-expansion` 5.0.9 / `undici` 8.9.0 advisory clearance on `next`.

**Excluded:** adopting `RemoteSession`/`PiClient` into production (verdict only — the build belongs to `shared-session-host-cutover`); adopting fullscreen TUI mode as a product feature (row C1 covers only the forced construction change); any new product capability; changes to the Pi source-alias dev workflow; `main`'s PR #422; provider-quality campaigns and KA-owned evidence.

### Source-of-truth inputs

The closed enumeration derives from three inputs, all available now: the distinct symbol set brunch imports from `@earendil-works/*` (import graph); the published 0.84.0–0.84.2 release notes; and direct `.d.ts` diffs of 0.83.0 (installed) against 0.84.1 plus 0.84.1 against 0.84.2 (both packed to scratch). The type surfaces caught row C1 and the 0.84.2 SettingsManager row even though neither appeared as a published breaking change, which is why release notes and imports alone are insufficient.

### 0.84.2 execution-start refresh — 2026-08-14

- npm reports `0.84.2` as the latest common release of all three pinned packages; this sweep therefore targets exactly `0.84.2`, not the `0.84.1` available when it was first scoped.
- The incremental 0.84.1→0.84.2 `.d.ts` diff found no removal or required-signature break on a Brunch callsite. In-process `MessageUpdateEvent.message` remains required; Pi's JSON/RPC event instead regains constant-size cumulative `usage`.
- One new required adaptation surfaced: Pi added `SettingsManager.getDefaultTools()` and `getFullscreenExitOutput()`. Brunch deliberately audits the complete getter surface, so `BRUNCH_SETTINGS_AUDITED_GETTERS` must acknowledge both and the tool-default policy must remain sealed.
- Other additions amend existing component, runtime, and disposition rows below rather than opening another sub-seam: fullscreen search/exit behavior and optional search theme roles; explicit `sendUserMessage(..., { expandPromptTemplates })`; experimental strict schema sampling for default built-ins; and optional `ToolCall.namespace` / `AssistantMessage.endTurn` diagnostics.

### Classification

**Evidence-gated.** The inventory is fully enumerable now, but the decisive rows cannot close by type-checking: B1/B2 read Pi events through structural casts that no type-aware lint sees through, so they need fresh witness evidence on the new baseline. Row C1 by contrast fails loudly and is buildable-now once the bump lands.

### Aggregate definition of done

- Every `●` row is `have` or `built`; none remains `partial`, `spec`, or `new`.
- `npm run check` and `npm run verify:full` pass on `0.84.2`.
- The four `session-runtime-contract-*.slow.test.ts` witnesses and the required FE-1348 subset re-pass on the moved baseline.
- Every `G` row carries a recorded verdict — adopted, promoted with a named owner, or declined with rationale.
- No `●` row's closure rests on an untyped structural cast.
- `npm run check:release-pack` passes and the two Pi-local advisory families are cleared in `package-lock.json`.
- A25-L is updated to validated or falsified with evidence.

## Ledger

### A · Runtime, session construction, and auth

| Capability | Status | Req | Fill | Owner / next | Notes |
| --- | --- | --- | --- | --- | --- |
| Version pins + lockfile move to 0.84.2 | `partial` | ● | `earned` | `package.json`, `package-lock.json` | Three exact pins and the lockfile moved to 0.84.2; ordinary `npm install` materialized all three root Pi packages at 0.84.2, confirmed by their installed manifests and `npm ls --depth=0`. Parsed `package-lock.json` `packages` evidence: Pi-local `brace-expansion` is 5.0.9 and Pi-local `undici` is 8.9.0. Closure is blocked on the moved baseline: `npm run check` exits 1 on row C1's `TUI` class→interface failures (`TS1484`/`TS2693` across production and test construction sites) and row A3's null-preserving `ProviderHeaders` incompatibility at `src/.pi/extensions/compaction/registrar.ts:40` (`TS2345`). Those rows own the fixes; A1 must remain partial until the sweep restores the check. Release intent: patch changeset `.changeset/fresh-pi-baseline.md`. |
| `ModelRuntime` / `ModelRegistry` construction + `refresh()` | `built` | ● | `earned` | `src/probes/faux-provider.ts:35`, `src/probes/executor-agent-runner-witness.ts:99` | **Declined:** do not surface `ModelsRefreshResult.errors` at either deterministic static faux-provider callsite. Pi 0.84.2 documents and implements refresh errors for configured dynamic providers; static providers are ignored, so no current reader or actionable failure mode benefits from plumbing an errors map that cannot diagnose these registrations. Both callsites immediately assert `getModel(provider, modelId)`, which remains the smaller complete oracle for their actual requirement and fails with the requested identity when registration is absent. `ModelsRefreshOptions.allowNetwork` remains optional in `dist/models.d.ts`; installed `dist/models.js` defaults it to true, always runs the cache-only phase with false for dynamic providers, and skips the network phase when the caller passes false. Both callsites continue to pass `{ allowNetwork: false }`. Focused verification passed `src/dev/__tests__/faux-harness.test.ts` and `src/probes/__tests__/executor-agent-runner-witness.test.ts`: 2 files / 8 tests, no skips. No source or test adaptation was justified. |
| `getApiKeyAndHeaders()` null-preserving `ProviderHeaders` | `built` | ● | `earned` | `src/.pi/extensions/compaction/registrar.ts:24` | Brunch preserves string values while composing provider headers, then materializes Pi's public `compact()` boundary by omitting every key whose final value is `null`; deletion markers are consumed rather than stringified or forwarded into `SimpleStreamOptions`. The discriminating string-vs-null oracle and both existing native/characterization compaction suites pass: 2 files / 9 tests, no skips. `npm run check` passes with six pre-existing unbound-method warnings. `npm run verify` reaches the separately owned A8 getter-completeness oracle (1 failed, 2815 passed, 2 skipped) and stops before build; A3's focused suites remain green. |
| Direct `compact()` call site | `built` | ● | `earned` | `src/.pi/extensions/compaction/registrar.ts` | **Confirmed:** Pi 0.84.2 still publicly exports the positional `compact(preparation, model, apiKey, headers, customInstructions, signal, thinkingLevel, streamFn, env, retry?, callbacks?)` API, so Brunch's direct invocation remains sanctioned and needs no adaptation. Brunch supplies auth and provider resolution through the extension context's `ModelRegistry`, then passes the selected provider's `streamSimple` as `streamFn`; installed Pi invokes that callback as `(model, context, requestOptions)` rather than falling back to `completeSimple`. Its `createSummarizationOptions` carries `apiKey`, materialized headers, `env`, signal, and reasoning models' non-off thinking level into those request options, preserving auth/env/model/thinking and provider dispatch. The native and characterization compaction suites passed on 0.84.2: 2 files / 9 tests, no skips. `npm run check` passes with six pre-existing unbound-method warnings and three topology warnings. Verification-only: no source or test adaptation was justified. |
| Session construction factories | `built` | ● | `earned` | `src/app/`, `src/dev/tier-2-harness.ts` | **Confirmed:** Pi 0.84.2 preserves Brunch's complete session-construction chain without adaptation. `createBrunchAgentSessionRuntimeFactory` still satisfies `CreateAgentSessionRuntimeFactory`; its factory materializes `createAgentSessionServices(...)`, passes those services to `createAgentSessionFromServices(...)`, and returns the created session plus services/diagnostics. Production `runBrunchTui` and all tier-2 boot/resume/reboot helpers still enter that factory through `createAgentSessionRuntime(...)`; the direct faux harness continues to use the separately exported `createAgentSession(...)`. Focused verification passed `src/app/__tests__/brunch-tui.test.ts` and `src/dev/__tests__/tier-2-harness.test.ts`: 2 files / 61 tests, no skips. `npm run check` passed with six pre-existing unbound-method warnings and three topology warnings; `npm run build` passed. Verification-only: no source or test adaptation was justified. |
| `SessionManager` / JSONL entry types | `built` | ● | `earned` | `src/session/` | **Confirmed:** Brunch's Pi 0.84.2 consumption of `SessionEntry`, `SessionHeader`, `SessionMessageEntry`, and `CustomEntry` remains viable without adaptation. The JSONL viability suite proves durable user/assistant payloads, custom entries/messages, provider-context re-entry, continuity/compaction metadata, and structured elicitation survive append + physical reopen as valid Pi entries. The directly relevant tree suites prove the persisted active root-to-leaf branch wins over abandoned siblings in transcript and exchange projection, branch-summary publication physically reopens at the selected leaf, and `createBranchedSession()` publishes a reopenable child with a valid `SessionHeader.parentSession` and inherited binding. Workspace coordinator coverage additionally proves multiple sessions in one cwd retain distinct ids/bindings and reload correctly, so Pi's per-cwd id scoping does not disturb Brunch identity. Focused verification passed 5 files / 74 tests with no skips: `jsonl-session-viability`, `active-session-branch`, `session-transcript`, `exchange-projection`, and `workspace-session-coordinator`. These append/branch/fork then reopen or read the durable file, providing behavior evidence across the upstream atomic publication paths rather than import-only compatibility. Verification-only: no source or test adaptation was justified. |
| `InMemoryCredentialStore` | `built` | ● | `earned` | `src/dev/`, probes | **Confirmed:** all three Brunch construction sites remain deterministic on Pi 0.84.2 without adaptation: `src/probes/faux-provider.ts:31` and `src/probes/executor-agent-runner-witness.ts:92` create an empty per-run store, then register a static faux provider and refresh/read its model; `src/dev/tier-2-harness.ts:301` creates an empty per-runtime store for the no-model harness and overrides availability to empty. No site retains the store, calls `login()`, `logout()`, `setRuntimeApiKey()`, or `removeRuntimeApiKey()`, shares it across runtimes, or performs any credential mutation, concurrent or otherwise. **Declined:** do not import, catch, or expose Pi's new `CredentialSynchronizationError`; Pi documents it for a credential mutation that committed but failed local model synchronization, and no current Brunch caller invokes that mutation surface. Adding a queue, catch path, or concurrency oracle would invent machinery absent a caller. Focused post-bump verification passed `faux-harness`, `executor-agent-runner-witness`, and `tier-2-harness`: 3 files / 22 tests, no skips. `npm run check` passed with six pre-existing unbound-method warnings and three topology warnings. Verification-only: no source or test adaptation was justified. |
| `SettingsManager` getter/default-policy refresh | `built` | ● | `earned` | `src/app/pi-settings.ts`, `src/app/__tests__/brunch-tui.test.ts`, `src/app/__tests__/pi-session-options.test.ts` | The live 0.84.2 completeness oracle found five new getters, correcting the stale two-getter framing: `getDefaultTools`, `getFullscreenExitOutput`, `getFullscreenScrollbar`, `getMermaidRenderingMode`, and `getTuiMode` are all acknowledged in `BRUNCH_SETTINGS_AUDITED_GETTERS` and none is pinned. **Declined:** do not adopt ambient default tools, fullscreen mode/exit policy/scrollbars, or a Brunch-specific Mermaid mode absent a product requirement. The live manager oracle preserves Pi's current defaults (`defaultTools` undefined, regular TUI, transcript exit output, auto scrollbar, streaming Mermaid) and Brunch's existing two-space markdown indent. The discriminating authority oracle injects hostile ambient `defaultTools: ['bash', 'edit', 'write']`, then proves production session projection remains explicit `noTools: 'builtin'` plus `excludeTools: ['bash', 'edit', 'write']`; settings therefore cannot widen Brunch's owned tool allowlists. Red: focused `brunch-tui` had 1 failed / 46 passed on all five missing getters. Green: focused settings/session-policy run passed 2 files / 50 tests, no skips. |

### B · Event and projection seam — highest risk

| Capability | Status | Req | Fill | Owner / next | Notes |
| --- | --- | --- | --- | --- | --- |
| In-process cumulative `message` on `message_update` | `built` | ● | `proving` | `src/projections/session/live-session-events.ts` | **Adopted:** the projection now consumes Pi's public typed `assistantMessageEvent.text_delta` directly and emits each upstream `delta` unchanged under the active `run:<n>` anchor. It no longer reads, accumulates, or prefix-diffs cumulative assistant `message` snapshots, and it adds no compatibility or dual-shape path; non-text and malformed update rivals fail closed. The adapter and runtime-contract fixtures emit the same public typed delta shape. Red: the stale cumulative adapter fixture produced only settlement (1 failed / 5 passed). Green: focused projection + adapter passed 2 files / 6 tests; named companion + structured-ask witnesses passed 2 files / 15 tests; the complete core-slow lane passed 9 files / 87 tests; `npm run verify:full` passed default 340 files + 1 skipped / 2817 tests + 2 skipped, core-slow 9 / 87, comparison 5 / 32, and build. |
| Subagent stream-update read | `built` | ● | `proving` | `src/.pi/extensions/subagents/session.ts` | **Adopted:** child-session subscriptions now carry Pi's public `AgentSessionEvent` and produce message previews directly from `assistantMessageEvent.text_delta`; the cumulative assistant `message` parser and structural casts are gone, with no compatibility or dual-shape path. The discriminating stream oracle proves the child preview text is emitted while thinking deltas and malformed non-string text rivals fail closed. Red: the typed delta event produced only lifecycle statuses (1 failed / 3 passed). Green: focused stream-output passed 1 file / 4 tests; the complete subagent suite passed 2 files / 7 tests; `npm run verify` passed 340 files + 1 skipped / 2818 tests + 2 skipped and build. |
| Wire-side JSON/RPC event consumers | `built` | ● | `proving` | `src/dev/__tests__/web-driver-streaming*.ts` | **Confirmed through the real `runBrunchTui` sidecar/host path:** canonical `/rpc` carries target-addressed `brunch.liveSessionEvent` frames whose `assistant_text_delta` chunks assemble exactly to the faux-provider reply and whose flushed JSONL contains the same text. The reconnect witness is contrastive: transitional `/rpc/driver` emits raw `brunch.sessionEvent` `message_update` frames and supports the deliberate mid-turn drop, while a client reconnected to canonical `/rpc` receives no replay, refetches equal canonical projections, then receives only semantic live-session frames for the next turn (`sessionFrames()` remains empty). This rules out silently relaying Pi 0.84.2 JSON events, including their cumulative `usage`, on the canonical wire. Focused verification passed streaming + reconnect: 2 files / 2 tests, no skips. Verification-only: existing discriminating oracles already proved the row, so no source or test adaptation was justified. |
| Typed-seam hardening for B1–B3 | `built` | ● | `earned` | `src/projections/session/live-session-events.ts`, `src/.pi/extensions/subagents/session.ts`, `src/dev/__tests__/web-driver-streaming-support.ts`, `src/rpc/` | The complete B1–B3 audit now follows `docs/praxis/pi-types.md`: B1 and B2 consume Pi's public `AgentSessionEvent` and narrow typed `assistantMessageEvent.text_delta`; B3's transitional relay and witness support carry `AgentSessionEvent`, while canonical `/rpc` carries Brunch-owned `LiveSessionEventFrame` semantic deltas. The audit found three remaining load-bearing structural casts in B3 witness helpers (assistant transcript assembly, tool lifecycle detection, and ask args); all now narrow the public discriminated union directly, so a future Pi event-shape change reaches the type checker. Local `hasSubscribe` trust-boundary validation, malformed-event fixture casts, and JSONL parse validation remain intentionally unchanged. `docs/praxis/pi-types.md` remains current and needed no reconciliation. Focused B1–B3 verification passed 5 files / 12 tests, no skips; `npm run verify` passed 340 files + 1 skipped / 2818 tests + 2 skipped and build. |

### C · pi-tui component surface

| Capability | Status | Req | Fill | Owner / next | Notes |
| --- | --- | --- | --- | --- | --- |
| `TUI` class → interface | `built` | ● | `earned` | 13 files, 30 former `new TUI(` sites | **Breaking, and absent from the published Breaking Changes list.** All 30 non-fullscreen constructions now use Pi's public `TuiMainScreen` export; no fullscreen behavior was adopted. Focused component/harness verification: 10 files passed, 1 skipped; 77 tests passed, 1 pre-existing skipped. `npm run check` now reaches only row A3's separately owned `ProviderHeaders` TS2345 failure. |
| `Markdown` / `MarkdownTheme` / `getMarkdownTheme` | `built` | ● | `proving` | `src/.pi/components/exchange-markdown-body.ts`, `cards.ts` | 0.84.0 added Mermaid and LaTeX rendering inside markdown. Brunch's current exchange/card semantics remain correct without adopting or styling those capabilities: all 25 focused component test files passed (132 tests), including the existing snapshot, and the explicit card/exchange-answer/component-preview subset passed (3 files, 6 tests). No rendered output or golden changed. |
| `Editor` / `CustomEditor` / autocomplete | `built` | ● | `earned` | `src/.pi/components/brunch-editor.ts`, `mode-input.ts`, `exchange-answer-editor.ts` | Brunch preserves its existing editor semantics on Pi 0.84.2 without adaptation. The focused real-TUI/editor subset passed 4 files and 17 tests with no skips, covering typed input, programmatic text, boxed autocomplete at multiple widths, multiline input, submit, cancel/Escape, empty-submit warning, and inherited Ctrl-D behavior. |
| Keybindings surface | `built` | ● | `earned` | `src/app/pi-keybindings.ts` | Brunch's existing policy and input semantics need no adaptation on Pi 0.84.2. Focused verification passed 3 files and 31 tests with no skips, covering the live `KeybindingsManager` policy, `TUI_KEYBINDINGS`-backed editor input, `matchesKey`, and the component-preview `isKeyRelease` guard. Default audit: prompt-history and fullscreen line-scroll remain unbound; transcript search is fullscreen-only. Its `ctrl+g` next-match key overlaps Pi's `app.editor.external`, but the fullscreen viewport listener owns that key only while search is active, and Brunch remains on regular `TuiMainScreen`; therefore no active Brunch binding is shadowed. `npm run check` reaches only row A3's separately owned `ProviderHeaders` TS2345. |
| `Terminal` / `ProcessTerminal` / width utils | `built` | ● | `earned` | `src/.pi/components/`, `virtual-terminal.ts` | Brunch preserves its virtual-terminal-backed rendering and input semantics on Pi 0.84.2 without production adaptation. All 7 focused virtual-terminal/component/editor files passed (21 tests, no skips): a lone Escape still reaches `CustomEditor.onEscape`, while an `ESC+b` Alt sequence moves by one word without firing Escape. `PI_TUI_ESC_TIMEOUT` therefore remains an upstream `ProcessTerminal` latency control; Brunch's test terminal correctly forwards already-buffered chunks and needs no timeout emulation. Focused width oracles pin the changed Indic grapheme widths (`क्ष` = 2, `क्षेत्र` = 4) and OSC 8 truncation: `truncateToWidth` keeps the link over retained text, closes it before the ellipsis, and `visibleWidth` remains bounded. The broader width/render subset passed 10 files (43 tests, no skips). `npm run check` reaches only row A3's separately owned `ProviderHeaders` TS2345. |
| Theme surface | `built` | ○ | `earned` | `src/.pi/themes/`, `src/dev/component-preview/theme.ts` | **Declined:** do not adopt optional `scrollbarThumb`, `searchMatchText`, or `searchMatchBg` roles while Brunch remains on `TuiMainScreen` without transcript search/fullscreen theming. Both shipped themes intentionally omit them; no colors were added for symmetry. The focused component-preview compile/render check passed 2 files and 26 tests with no skips, exercising both shipped variants and the real theme testbed. `npx tsc -p tsconfig.build.json --noEmit` and `npm run check` reach only row A3's separately owned `ProviderHeaders` TS2345; neither reports a theme-surface failure. No production or fixture adaptation was necessary. |

### D · Extension surface

| Capability | Status | Req | Fill | Owner / next | Notes |
| --- | --- | --- | --- | --- | --- |
| `ExtensionAPI` / context shape / event-bus disposal | `built` | ● | `earned` | `src/.pi/extensions/` | Brunch's extension registrations and lifecycle behavior remain compatible with Pi 0.84.2; no production adaptation is needed. The complete focused extension suite passed 43 files / 446 tests with no skips. A lifecycle-focused subset (`ask-runtime-mount.test.ts`, `session-hooks-lifecycle.test.ts`, `registry.test.ts`) passed 3 files / 59 tests with no skips, covering real mounted ask cleanup, session-boundary handler registration, and the full extension registration inventory. Listener/disposal verdict: Brunch installs handlers through Pi's `ExtensionAPI.on` and does not retain a parallel event-bus subscription to clean up; Pi owns removal on reload/disposal, while Brunch-owned child-session subscriptions still unsubscribe before `session.dispose()` and their focused tests pass. `npm run check` reaches only row A3's separately owned `ProviderHeaders` TS2345. |
| Tool definitions and built-in tool factories | `built` | ● | `earned` | `src/.pi/extensions/`, executor | Brunch's `defineTool` schemas and both built-in consumer forms (`create{Find,Grep,Ls,Read}Tool` in the foreground runtime; `create{Find,Grep,Ls,Read}ToolDefinition` in the sealed subagent catalog) remain compatible with Pi 0.84.2 without production adaptation. The focused authority/runtime/subagent/schema set passed 8 files / 90 tests with no skips. Behavior-bearing factory probes passed for both find forms: `path: nested` + `**/*.ts` returned `hit.ts`, while root `path: .` + `nested/*.ts` returned `nested/hit.ts`; grep path+glob, relative ls, and relative read also returned the expected fixture content. Runtime validation accepted valid nested `anyOf` and `oneOf` string/number members and rejected invalid object members; Brunch's schema suite additionally proves top-level provider-illegal unions are rejected while nested unions remain legal. `npm run check` reaches only row A3's separately owned `ProviderHeaders` TS2345. |
| Skills and resource loading | `built` | ● | `earned` | `src/agents/skills/registry.ts` | Brunch's `loadSkills` registry and sealed `ResourceLoader` / `DefaultResourceLoader` consumption remain compatible with Pi 0.84.2 without production adaptation. The registry suite passed 1 file / 7 tests with no skips: all eight Brunch-owned skills load from explicit absolute `src/agents/skills/<id>/SKILL.md` paths, including ingest's nested `../map/references/routing.md` resource, and still resolve from an unrelated process cwd. The sealed resource subset passed 1 file / 6 selected tests (41 unselected) and the faux runtime passed 1 file / 5 tests with no skips; explicit Brunch themes and extension factories load while ambient skills, prompts, extensions, themes, settings, and `APPEND_SYSTEM.md` remain excluded. Recursive-path verdict: fixed upstream behavior preserves the intended explicit roots without cwd-relative widening. Malformed-array verdict: Brunch does not admit ambient resource arrays at this boundary (`SettingsManager.inMemory` pins valid owned arrays and every `no*` resource seal is enabled), so malformed ambient arrays cannot reach startup; real-loader reload and faux-session startup complete without a crash. The sealed subagent/system-prompt runtime pair additionally passed 2 files / 61 tests with no skips. `npm run check` reaches only row A3's separately owned `ProviderHeaders` TS2345. A broader three-file test invocation exposed the separately owned A8 SettingsManager getter-audit failure (five unaudited 0.84.2 getters); D3 did not alter or absorb it. |
| 0.84.2 session dispatch and system-prompt fixes | `built` | ● | `earned` | `src/.pi/extensions/`, `src/agents/runtime/`, `src/app/` | Brunch remains correct on Pi 0.84.2 without production adaptation. The focused command/runtime-system-prompt suites passed 2 files / 46 tests with no skips; the RPC prompt adapter suite passed 1 file / 4 tests with no skips. Prompt audit: all eight production `session.prompt` machine-control paths explicitly pass `{ expandPromptTemplates: false, source: 'rpc' }` (`tui-live-session-adapter.ts`, `brunch-web.ts`, `brunch-tui.ts`, `introspection-launcher.ts`, `faux-launcher.ts`, `generate-fan-out-witness.ts`, `tier-2-harness.ts`, and subagents `session.ts`). The sole `ExtensionAPI.sendUserMessage` call is `structured-exchange-ordering-proof.ts:194`, whose literal Brunch-owned proof text needs no expansion; the four `AgentSession.sendUserMessage` proof/curation callsites likewise send Brunch-assembled text and default to no expansion. **Verdict — declined:** do not opt into `sendUserMessage(..., { expandPromptTemplates: true })`; ambient command/skill/template dispatch would violate the machine-control seal, while explicit `false` is optional because 0.84.2 declares the default false. Dispatch audit: production `ExtensionAPI.sendMessage` has one inert custom-card append with no trigger option and one orientation adapter whose type admits only `triggerTurn: true`; there is no explicit `triggerTurn: false` callsite. Pi 0.84.2's installed `AgentSession.sendCustomMessage` implementation routes omitted/false triggers to append + `message_start`/`message_end`, never `_runAgentPrompt`, while only truthy `triggerTurn` takes the turn branch; Brunch's inert-send and explicit-kick semantics therefore remain correct. Custom prompt composition remains correct: the runtime suite proves the Brunch prompt follows the base prompt exactly once and repairs provider payload carriers, including the trigger-turn bypass, without changing tools. `npm run check` exits 1 only on row A3's separately owned `ProviderHeaders` TS2345 (plus six pre-existing unbound-method warnings); D4 did not absorb A3 or A8. |

### E · Test substrate and witnesses

| Capability | Status | Req | Fill | Owner / next | Notes |
| --- | --- | --- | --- | --- | --- |
| Faux provider registration | `have` | ● | `earned` | `src/probes/faux-provider.ts` | **Verified no-impact at scope time:** brunch's `ProviderConfig` is a static `models` array plus an optional `streamSimple` override — no `refreshModels`, no `context.store`. The provider-refresh `store`→`stored`/`publish()` break does not reach it. Closure: faux-harness suite green. |
| `InteractiveMode` production composition | `built` | ● | `proving` | `src/app/brunch-tui.ts` | **Confirmed through the production normal-TUI path:** Pi 0.84.2 preserves D141-L's real `InteractiveMode` composition and I64-L's synchronous `process.on('exit')` writer release without adaptation. The tracer supplies no launcher override, boots the real Brunch/Pi chrome and editable prompt in a PTY, completes one deterministic turn through the substituted backend, persists the sole canonical JSONL exchange, then proves bounded Ctrl-D shutdown removes both the PTY session and target writer lock. Focused verification passed `session-runtime-contract-tracer.slow.test.ts`: 1 file / 4 tests, no skips. Verification-only: no source or test adaptation was justified. |
| The four convergence witnesses | `built` | ● | `proving` | `src/app/__tests__/session-runtime-contract-*.slow.test.ts` | **Confirmed on Pi 0.84.2 without witness or production adaptation:** tracer, companion, structured-ask, and authority remain the convergence oracles for I64-L/I65-L and discriminate against a silent return to direct Pi-delta relay. An exact core-slow equivalent ran only these four files with the lane's `--maxWorkers=1`: 4 files / 31 tests passed, no skips. Verification-only: no source or test adaptation was justified. |
| Required FE-1348 subset re-run | `built` | ● | `proving` | FE-1348 retained evidence | **Confirmed on Pi 0.84.2 without oracle or production adaptation.** The differential selects only retained FE-1348 automated leaves whose product paths consume Pi session/runtime surfaces: SW1 lifecycle/projection/React (`brunch-web-process-lifecycle`, `session-presentation`, `session-route`, `live-overlay`); stdio RPC structured-exchange recovery/projection (`handlers`, `structured-exchange-loop`, `exchange-projection`, `recovery`); CS1/SW2/SW3 review-set presentation and settlement (`exchanges-present-request`, `review-set`, `accept-review-set`, `review-set-settlement`, plus the shared presentation/React leaves already selected); and final Execute availability through Pi command/orientation registration (`commands-runtime-switch`, session-orientation `registrar`, `process-move-availability`). This mapping derives from `TESTING_FINDINGS.md`'s fixed dispositions and named repair commits, `TESTING_PLAN.md`'s current concern map, the archived FE-1348 closeout, and their linked walkthrough canonical-oracle sections; it deliberately excludes human/provider/browser reruns, aggregate repository/package/CI/comparison checks, no-impact rows, and E2/E3's four convergence witnesses. One focused invocation passed 15 files / 268 tests with no skips or divergences. Verification-only: no source or test adaptation was justified. |

### F · Verified no-impact (closed at scope time, evidence recorded)

| Capability | Status | Req | Fill | Owner / next | Notes |
| --- | --- | --- | --- | --- | --- |
| agent-core v4 `Session`/`SessionStorage`/`SessionRepo` | `have` | ● | `earned` | — | Largest changelog item. Zero call sites: no `AgentHarness`, `SessionRepo`, `JsonlSessionRepo`, or `InMemorySessionRepo` anywhere in `src/`. **Post-bump reconfirmed on Pi 0.84.2:** production `src/` grep for all four symbols returned zero matches (`rg` exit 1), so `have` is preserved without re-audit or adapters. |
| Required `FileSystem.renameFile()` | `have` | ● | `earned` | — | No custom harness `FileSystem` implementation exists. **Post-bump reconfirmed on Pi 0.84.2:** production `src/` grep for `FileSystem` or `renameFile` returned zero matches (`rg` exit 1), so `have` is preserved without re-audit or an adapter. |
| `ModelsStreamTransforms` → `ModelsRequestTransforms` | `have` | ● | `earned` | — | Zero call sites. **Post-bump reconfirmed on Pi 0.84.2:** production `src/` grep for either transform type returned zero matches (`rg` exit 1), so `have` is preserved without re-audit or adapters. |
| `setRuntimeApiKey()` signature | `have` | ● | `earned` | — | Zero call sites on `next`; this is `main`/PR #422's migration, not ours. **Post-bump reconfirmed on Pi 0.84.2:** production `src/` grep for `setRuntimeApiKey` returned zero matches (`rg` exit 1), so `have` is preserved without migration or re-audit. |
| `RemoteSession.sessions` → `SessionMetadata` | `have` | ● | `earned` | — | Zero call sites; relevant only if G1 adopts the client surface. |
| 0.84.2 no-impact type surface | `have` | ● | `earned` | — | Zero direct callsites for breaking `ensureTool()` callback-signature change or additive `ScrollView.scrollTo()` options, Cloudflare binding helpers, constrained-sampling helpers, and fullscreen search classes. Optional `ToolCall.namespace` and `AssistantMessage.endTurn` are safely ignored by current transcript/projection readers. Re-confirm by grep after the bump; do not create adapters. |

### G · New-API disposition — verdict required, adoption not assumed

Each row closes on a recorded verdict: **adopted** (folded into this sweep), **promoted** (named owner + re-entry trigger), or **declined** (rationale). Adoption is in-scope here only when it removes an existing brunch workaround or is required for parity.

| Capability | Status | Req | Fill | Owner / next | Notes |
| --- | --- | --- | --- | --- | --- |
| `PiClient` / `RemoteSession` / CBOR / Unix-socket transport | `new` | ● | `proving` | `shared-session-host-cutover` | **Load-bearing.** Owed to the `shared-session-host-convergence` arc *before* it deletes `SessionEventRelay` and `/rpc/driver`. Does not by itself un-retire A47-L — it is a client for a remote session and says nothing about releasing `InteractiveMode.run()`. Verdict must state whether upstream now ships the seam the arc plans to hand-build. |
| `assistantMessageEvent` deltas as the direct source | `new` | ● | `proving` | row B1 | Upstream removed the cumulative field because it grew quadratically; brunch's prefix-diff re-derivation now works against the grain. Strongest adopt-now candidate — it removes a real workaround and retires the B1 hazard at its root rather than guarding it. |
| `pi.registerMarkdownTransformer()` | `new` | ● | `earned` | `src/.pi/components/` | Display-only markdown transformation; brunch owns heavy custom rendering. Verdict only. |
| `tool_call` `terminate` | `new` | ● | `earned` | executor | Lets all-terminating batches skip the follow-up model call. Verdict only. |
| `AGENTS.override.md` per-directory context | `new` | ● | `earned` | comparison harness | Bears on target isolation in `/compare-specs`. Verdict only; adoption would belong to `comparison-machine-interface-cutover`. |
| `pi auth check` | `new` | ● | `earned` | comparison harness, release-pack smoke | Credential preflight. Verdict only. |
| `AgentOptions.shouldStopAfterTurn` | `new` | ● | `earned` | executor | Graceful stop after a completed turn. Verdict only. |
| Vendor-neutral telemetry contracts | `new` | ● | `earned` | Horizon `mechanism-trace` / `agent-tracing` | Verdict only; those Horizon items are trigger-gated and this is a candidate trigger. 0.84.2's optional `ToolCall.namespace` and `AssistantMessage.endTurn` are related diagnostics inputs, not reasons to build telemetry here. |
| Configurable `defaultTools` + experimental strict built-in sampling | `new` | ● | `earned` | row A8, tool policy | Verdict required. Brunch currently disables built-ins or supplies explicit allowlists, and strict sampling covers Pi's default read/bash/edit/write tools only; adoption must not widen Brunch tool authority. |
| `sendUserMessage(..., { expandPromptTemplates })` | `new` | ● | `earned` | row D4 | Verdict required. Product machine-control paths intentionally suppress ambient command/resource expansion; adopt only if a named Brunch-owned command-dispatch path needs it. |
| `samplingParams` / vLLM `thinking_token_budget` | `new` | ○ | `earned` | model config | Deferred — no current Brunch need. Record the decline. |
| Fullscreen TUI mode, transcript search, exit output, and per-run theme | `new` | ○ | `earned` | Horizon | Deferred and explicitly out of boundary. Rows C1/C4/C6 and E2 cover only forced compatibility and regression checks, not adopting fullscreen product behavior. |

## Row discipline

- Rows E2 and E3 re-confirm existing contracts by rerunning their named witnesses. F rows remain closed unless their named post-bump grep/reconfirmation contradicts the recorded no-impact evidence.
- Rows B1–B3 do **not** close on a green type-check. Their oracle is a witness that asserts output is *produced*, not merely that nothing throws.
- A row that turns out to need its own full card spawns a sibling `single` file under `memory/cards/`; leave the pointer in that row's Owner cell rather than fattening this ledger.
- The list is closed at the 0.84.2 execution-start target. Scoping first widened it for C1 and the C/D/E sub-seams; the 2026-08-14 refresh added only the required SettingsManager policy row and folded every other 0.84.2 delta into existing sub-seams. If execution discovers **more than one** further genuinely-missing row or a new sub-seam, stop and route back through `ln-plan`.

## Execution order

1. Land the version pins and lockfile (row A1); capture the advisory-clearance evidence immediately, before any adaptation muddies the diff.
2. Take the loud failures first: row C1, then the rest of C and D. These fail at the type checker and bound the mechanical work.
3. Take A2–A8 — small, settled, type-guided; the SettingsManager completeness test should make A8 loud.
4. Take B1–B3 with witnesses, not types. Then B4 hardens the seams so the next bump is loud.
5. Re-run E2/E3, then the required FE-1348 subset as a differential.
6. Record every G verdict. Promote what needs design; decline what does not, with rationale.
7. Reconcile `memory/PLAN.md` and A25-L in `memory/SPEC.md` to the evidence actually obtained.

## Expected touched paths (tentative)

```text
package.json                                              ~
package-lock.json                                         ~
.changeset/                                               +
src/
├── projections/session/live-session-events.ts            ~   # B1
├── .pi/
│   ├── extensions/subagents/session.ts                   ~   # B2
│   ├── extensions/compaction/registrar.ts                ~   # A3
│   └── components/
│       ├── workspace-dialog/preflight.ts                 ~   # C1 production
│       ├── exchange-markdown-body.ts                     ?   # C2 goldens
│       └── __tests__/                                    ~   # C1 harnesses
├── dev/
│   ├── component-preview.ts                              ~   # C1 production
│   └── __tests__/web-driver-streaming*.ts                ~   # B3
├── probes/
│   ├── faux-provider.ts                                  ~   # A2
│   └── executor-agent-runner-witness.ts                  ~   # A2
└── app/
    ├── pi-settings.ts                                    ~   # A8
    ├── __tests__/brunch-tui.test.ts                      ~   # A8
    └── __tests__/session-runtime-contract-*.slow.test.ts ?   # E3, only if contracts move
memory/
├── PLAN.md                                               ~
├── SPEC.md                                               ~   # A25-L disposition
└── cards/pi-084-upgrade--change-surface-ledger.md        ~
```
