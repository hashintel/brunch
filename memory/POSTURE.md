posture: prototype

# overrides — where brunch diverges from the prototype preset:
stakes: high                # POC handles real workspace/session/graph data — validate at boundaries
horizon: current-milestone  # stubs and layouts may extend across the current milestone, not just one slice
