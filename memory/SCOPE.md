# Scope: Workspace Semantic Shell Scaffolding

> Status: retired as a standalone scope card on 2026-04-14.

This umbrella card described the broader shell-honesty pass that has already been split and mostly absorbed into the live frontier.

## Why It Is Retired

- The shipped shell pass already covered the coarse workspace-semantic fixes this card was created to drive.
- The remaining work was decomposed into sharper seams in `memory/PLAN.md`: terminal staging first, then turn lifecycle, then turn-linked capture projection.
- Keeping this full card live alongside `SPEC` and `PLAN` only adds a second, stale description of the same frontier.

## Use Instead

- Current first build card: `memory/SCOPE_PHASE_TERMINAL.md`
- Canonical frontier: `memory/PLAN.md`
- Historical implementation/detail trail: git history, especially commit `9667009`
