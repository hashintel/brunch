# Label mapping — OUTSIDE the outcome judge packet

Drawn randomly (recorded in session shell output) before packet assembly.

- A = claude-c5 (Claude Code CLI lane)
- B = brunch-b2 (Brunch TUI lane)

Do not include this file or its contents in the outcome packet or outcome judge context.
