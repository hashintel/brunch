# Addendum 04 — Second-operator handover witness

> Field-only shell for the future independent operator. Complete from direct
> observation; do not infer or backfill values from earlier FE-1210 runs.

## Operator attestation

- Operator name:
- Witness date:
- Independence — I did not author the FE-1210 operator-handover scope card:
- Independence — I did not author the comparison-runs entry-point edit:
- Independence — I did not execute the original FE-1210 tracer:
- No-coaching attestation — I received no live procedural coaching beyond mechanical environment or Safehouse authorization:
- Mechanical authorization received, if any:

## Launch configuration

- Harness / Pi session identifier:
- `.pi/interactive-shell.json` values observed:
  - `minQueryIntervalSeconds`:
  - `handsFreeQuietThreshold`:
  - `handsFreeUpdateMaxChars`:
- Hands-free launch values observed:
  - `quietThreshold`:
  - `updateInterval`:
  - `autoExitOnQuiet`:
- Cursor spawn request:
- Cursor adapter command / model observed:
- Safehouse or executable-path configuration observed:
- Smoke input sent:

## Push-first exchange

- Session launch timestamp:
- Startup-only quiet wake timestamp, if any:
- Startup-only wake observation, if any:
- Input send timestamp:
- First post-input pushed-wake timestamp:
- First post-input pushed output (bounded, verbatim):

  ```text

  ```

- Send-to-wake latency:
- Status queries before first post-input wake:
- Post-wake bounded current-tail read used (yes/no):
- If used, reason the pushed tail was ambiguous:
- If used, bounded current-tail observation:

  ```text

  ```

## Cleanup

- Final target process status:
- Final overlay session status:
- Kill action, if needed:
- Dismiss action, if needed:
- Empty background-session check:
- Cleanup timestamp:

## Historical boundary

- Attestation — this addendum records post-run FE-1210 handover evidence only; it does not add a retroactive Cursor campaign lane or amend the historical two-lane outcome or process judgments:
- Operator signature / typed confirmation:
