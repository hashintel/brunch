# cursor-x1 — validity note (lane skipped)

**Status: SKIPPED (environment) — optional lane, declared, no attempt launched.**

The Cursor CLI lane is best-effort in the mission packet and the tracer card. At
probe time (2026-07-16T19:07Z) `~/.local/bin/cursor-agent` was a broken symlink to
`~/.local/share/cursor-agent/versions/2026.07.08-0c04a8a/cursor-agent`, and the
versions directory contained no installed builds. Launching the lane would have
required installing or repairing software mid-campaign, which is outside the frozen
actor recipe's remit (mechanical launch recovery is allowed; provisioning a target
is not).

No session was started, no mission delivery occurred, no budget was consumed, and
no target cwd was created for this lane. The comparison proceeds Brunch-versus-
Claude as the packet's required pair; judges should treat the Cursor column as
absent, not failed.
