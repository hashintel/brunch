# brunch-b2 — cleanup status

- Session `brunch-b2` killed cleanly at 2026-07-16T18:28:30Z after 19m39s (final
  viewport: settlement summary, 16 committed nodes, idle prompt).
- Raw target cwd: `<ephemeral-workspace>` (normalize to
  `<ephemeral-workspace>` on promotion per I55-L). Contents after lane:
  `mission-public.md`, `locker-pickup-spec.md` (document-export output), `.brunch/`
  workspace state (diagnostic only; never enters judgment).
- `document-export` exit 0; output 4534 bytes; copied to lane evidence as
  `final-document.md`.
- No controller path or reveal-key content ever entered the target cwd, opening
  prompt, or any visible exchange.
- Background sessions after kill: to be re-verified before the next lane launch
  (claude-c1).
