# Addendum 01 — attempt brunch-b2 (deviation record)

The manifest is immutable; this addendum names the deviation per its own rule.

- **Deviation:** the Brunch lane gets a second attempt, `brunch-b2`, after attempt
  `brunch-b1` exhausted its elapsed budget through a documented actor observation
  error (see `brunch-b1/validity-note.md`). `brunch-b1` is retained in full.
- **Unchanged:** mission `fictional-library-lockers-v1` verbatim, matched budgets
  (3 questions / 8 turns / 20 min / 1 intervention), frozen reveal policy and key,
  target order (Brunch before Claude), acquisition rule (`document-export` from
  settled graph state only), validity rules.
- **Changed:** fresh Brunch target cwd (new temp dir; `brunch-b1`'s cwd is evidence,
  not reusable — its workspace saw the mission), and corrected actor observation
  discipline: tail-only viewport reads, answers to qualifying questions prepared
  controller-side before launch, no keystrokes except deliberate policy actions.
- **Why this is not a selective rerun:** the failure was actor-mechanical, not
  target performance; the failed attempt stays visible in scratch and in any
  promoted bundle alongside `brunch-b2`, whatever `brunch-b2`'s outcome.

Declared 2026-07-16T18:08Z, before `brunch-b2` launch.
